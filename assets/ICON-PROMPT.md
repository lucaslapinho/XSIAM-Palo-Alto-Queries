# Cortex XQL Library icon

Generation mode: built-in `image_gen` tool. No CLI or external image API runner was used.

Use case: `logo-brand`, followed by one `precise-object-edit` refinement.

Created: 2026-09-11.

Final asset: `icon.png`, a 1254 x 1254 RGB PNG. The generated size is retained without post-processing.

SHA-256: `908afb8d03a3d1410e806bbdbd55850ba9ceabd4cff25ba3ce0f37c043926f2a`.

Visual direction: a bold shield outline containing a query prompt chevron and underscore. Requested palette: dark navy `#071B2E`, teal `#27D3BC`, and a small amber `#FFB547` accent. The project name is contextual and is not rendered in the image.

Reference images: none. The refinement used only the original draft generated for this project. No official Palo Alto Networks or GitHub logo was supplied or used as a reference.

Visual review: inspected the generated draft, removed its glow/halo through the built-in editor, and inspected the selected result. The final icon has a centered shield, visible `>_` motif, clean edges, generous surrounding space, and no words or watermark. Generated raster colors may vary slightly from the requested palette.

## Initial generation prompt

```text
Use case: logo-brand.
Asset type: original square PNG project icon for a community security-query repository called Cortex XQL Library; the name is context only and must not appear in the image.
Primary request: create one clean, confident geometric emblem combining a shield with a query prompt symbol >_. Render the greater-than chevron and a short underscore as geometric shapes, not typography.
Scene/backdrop: completely solid dark navy square background, edge to edge.
Subject and composition: a single centered thick teal shield outline with broad shoulders and a simple pointed base. Inside the shield, a bold teal greater-than chevron and a short amber underscore form a balanced query-prompt motif. Strong silhouette, ample negative space, even visual weight, all emblem elements comfortably inside a 15 percent safe margin. Recognizable as a tiny repository avatar.
Style/medium: flat vector-like raster artwork, crisp smooth edges, exact simple geometry, restrained professional technical identity.
Color palette: deep navy #071B2E background, vivid teal #27D3BC shield and chevron, small warm amber #FFB547 underscore accent. Flat solid colors only.
Constraints: one icon, square 1024 by 1024 composition, no words, no letters, no numbers, no captions, no watermark. No gradients, shadows, glow, bevels, 3D, texture, circuit-board details, extra badges or surrounding UI. Original project symbol only; no official Palo Alto Networks or GitHub logo and no imitation of an existing branded logo.
```

## Final refinement prompt

```text
Use case: precise-object-edit.
Edit target: the supplied original shield-and-query project icon.
Make one focused finish correction: convert the entire image to perfectly flat solid-color artwork. Remove every glow, halo, shadow, gradient, grain and edge texture. Replace the entire square background, including all area outside the shield, with one completely opaque and uniform dark navy #071B2E; the shield interior uses that exact same navy. Use uniform teal #27D3BC for the thick shield outline and greater-than chevron, uniform amber #FFB547 for the short underscore.
Keep the existing shield silhouette, chevron/underscore geometry, centered composition, strong stroke weights and padding unchanged. Preserve crisp antialiased edges. No additional objects, no words, no letters, no numbers, no captions, no branding, no watermarks. The final is a simple flat vector-like PNG, not a rendered glowing badge.
```
