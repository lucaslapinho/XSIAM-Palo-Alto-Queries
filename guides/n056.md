# N056 — Security Tool Disablement Requests - Schema Binding Template

Finds explicit stop, deletion, uninstall or protection-disable requests against time-valid inventoried security targets, preserving action outcome without asserting effective disablement.

Proposal: **TH-032**. Category: **Threat Hunting / Defense Impairment**.

Artifact: **SCHEMA_BINDING_TEMPLATE**. Product/surface: Cortex XSIAM / Interactive XQL Search. Tenant release, full-query compiler acceptance and execution: **UNVERIFIED / NOT RUN**.

[Query file](../queries/threat-hunting/defense-impairment/n056-security-tool-disablement-requests-schema-binding-template.xql)

## Objective and interpretation

One security-targeted action attempt and valid inventory target; no state-change success inferred.

Output grain: One security-targeted action attempt and valid inventory target; no state-change success inferred.
Timeframe: 1d unless explicitly expanded in body

## Source and field contract

Sources: `["{{SECURITY_CONTROL_ACTION_DATASET}}", "{{ENDPOINT_SECURITY_TARGET_INVENTORY}}"]`.

Every schema token must resolve to an inspected field, expression or source with the stated type and meaning. A normalized source contract may require telemetry ingestion, parsing or a separately reviewed view; replacing names alone cannot create missing evidence. Preserve nulls. Verify timezone, identity namespaces, provider/channel, event outcomes, source completeness and row multiplicity.

- **SECURITY_CONTROL_ACTION_DATASET** — {"type": "DATASET_IDENTIFIER", "kind": "DATASET", "binding_contract": "Audited commands/control requests with tokenized action and explicit service/package/product target; source-specific parser validation required.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CONTROL_ACTION_TIME** — {"type": "DATETIME", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Action request or command-start source time.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CONTROL_ACTION_UID** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Unique action/command event identifier. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CONTROL_ACTION_ENDPOINT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Target endpoint. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CONTROL_ACTION_ACTOR** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Initiating account. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CONTROL_ACTION_OPERATION** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Canonical stop_service, delete_service, uninstall_product or disable_protection only for documented action semantics; query/status/install operations differ. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CONTROL_TARGET_TYPE** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "service_name or product_id, matching inventory namespace. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CONTROL_TARGET_KEY** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Exact tokenized target identity, not a broad substring. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CONTROL_COMMAND_CONTEXT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Safe original command/action context; do not extract credential values. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CONTROL_REPORTED_RESULT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Source-reported action result; preserve unknown/failed/requested. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **ENDPOINT_SECURITY_TARGET_INVENTORY** — {"type": "DATASET_IDENTIFIER", "kind": "DATASET", "binding_contract": "Authoritative endpoint security service/package/product identity inventory, valid at action time.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CONTROL_INV_ENDPOINT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Target endpoint matching action. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CONTROL_INV_TYPE** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "service_name or product_id. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CONTROL_INV_KEY** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Exact security-target identity. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CONTROL_INV_PRODUCT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Security product. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CONTROL_INV_FROM** — {"type": "DATETIME", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Inclusive validity start.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CONTROL_INV_TO** — {"type": "DATETIME", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Exclusive validity end.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}

[Unfilled bindings file](../bindings/n056.bindings.example.json). All binding values intentionally start as null.

## Pipeline and result semantics

Read the stages in order: the source and early filters establish the population; alter creates explicitly typed aliases; joins/union introduce related evidence; comp/windowcomp/dedup change row cardinality where present; the final projection, sort and limit define displayed rows. A result limit does not bound scanning or join work. Check the identity and time predicates in each subquery against the binding contract.

Estimated cost: HIGH. Bounded lookback and early filters; result caps do not bound upstream scanning. No measured performance claim.

## Limitations and tuning

- Maintenance, upgrades and approved troubleshooting can legitimately match.
- Process launch/accepted job does not prove service stop, uninstall completion or impaired protection. Pivot to state/configuration evidence.
- General service operations are excluded unless the exact endpoint target is inventoried as security software.
- All bindings are explicit contracts, not claims that a source or field exists. Bind field/expression placeholders to typed tenant fields or tested extraction expressions; never substitute a made-up name.
- One-day telemetry lookback unless stated otherwise. Source-clock, retention, collection and permission gaps can remove evidence; _time filtering must cover each source event time.
- All canonical normalized words in expressions are local mapping contracts, not undocumented Cortex enum values.
- Result limits apply after analysis and do not cap scanned data or join fan-out. No tenant compile, execution, performance or deployment claim.
- ATT&CK, where present, is an analytical interpretation requiring context, not proof of compromise.
- Inventory/coverage/snapshot source bindings must return every relevant validity record under the configured query timeframe; if their storage timestamps differ, establish and test an explicit inner timeframe before use. A missing lookup row is never automatically benign.
- Comparisons are case-sensitive. Binding owners must canonicalize values only in source namespaces proven case-insensitive (such as reviewed Windows service/path/account keys and DNS A-label names), identically on both join sides. Preserve Unix paths, case-sensitive identities, ARN resource components, and source evidence text. All normalized operation/result labels must match the documented local contract spelling exactly.
- Time-difference bounds are expressed at second resolution. Validate subsecond boundary behavior using source fixtures; direct timestamp comparisons enforce before/after ordering where required.

## Acceptance scenarios — NOT RUN

- {"kind": "positive", "fixture": "A stop_service request explicitly targets a security service installed on H at the request time.", "expected": "Returned with reported result, including failures.", "status": "NOT RUN"}
- {"kind": "negative", "fixture": "A status query or stop of an unrelated application service occurs.", "expected": "Excluded.", "status": "NOT RUN"}
- {"kind": "null", "fixture": "Target cannot be parsed or endpoint security inventory is absent.", "expected": "Excluded; coverage gap remains.", "status": "NOT RUN"}
- {"kind": "edge", "fixture": "A security package was removed from inventory before a later unrelated package reuses its name.", "expected": "Expired inventory cannot label the later target as security software.", "status": "NOT RUN"}

These scenarios are plans for representative source fixtures and bounded tenant runs. They have not been executed in Cortex. First inspect the bound schema, then compile the complete body, then exercise positive, benign, negative, null and boundary cases. Do not promote this interactive hunt directly to BIOC/correlation without reviewing that surface.

Independent static review: **PASS**. Full-query C/E: **NOT RUN / NOT RUN**. Static review cannot establish source population or detection effectiveness.

## ATT&CK context

{"id": "T1685", "technique": "Disable or Modify Tools", "tactics": "Defense Impairment (TA0112)"}

Source workbook ATT&CK Enterprise v19.2 hypothesis retained for traceability. The query observes only the behavior in its coverage boundary; mapping is not certified detection coverage.

## References

- [MITRE Disable or Modify Tools](https://attack.mitre.org/techniques/T1685/)
- [Cortex action schema](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/action-actor)
- [Cortex XSIAM comp](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/comp?contentId=6TGIbW601nVvh2ycedN_hw)
- [Cortex XSIAM join](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/join?contentId=MyWFbdks6xgkqm_1lNkfGg)
- [Cortex XSIAM bin](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/bin?contentId=CU_u_bTkg_MICr5pUKivwg)
- [Cortex XSIAM sort](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/sort?contentId=a9tB71nqIWhA~Adcae6R7w)
