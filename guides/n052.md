# N052 — RDP Session Followed by PowerShell Behavior - Schema Binding Template

Links successful RemoteInteractive authentication to qualifying PowerShell process starts within 30 minutes using a validated endpoint and logon-session key.

Proposal: **TH-028**. Category: **Threat Hunting / Lateral Movement**.

Artifact: **SCHEMA_BINDING_TEMPLATE**. Product/surface: Cortex XSIAM / Interactive XQL Search. Tenant release, full-query compiler acceptance and execution: **UNVERIFIED / NOT RUN**.

[Query file](../queries/threat-hunting/lateral-movement/n052-rdp-session-followed-by-powershell-behavior-schema-binding-template.xql)

## Objective and interpretation

One authenticated session event and same-session process-start pair within 30 minutes; multiple qualifying starts remain separate.

Output grain: One authenticated session event and same-session process-start pair within 30 minutes; multiple qualifying starts remain separate.
Timeframe: 1d unless explicitly expanded in body

## Source and field contract

Sources: `["{{WINDOWS_AUTH_DATASET}}", "{{SESSION_PROCESS_START_DATASET}}"]`.

Every schema token must resolve to an inspected field, expression or source with the stated type and meaning. A normalized source contract may require telemetry ingestion, parsing or a separately reviewed view; replacing names alone cannot create missing evidence. Preserve nulls. Verify timezone, identity namespaces, provider/channel, event outcomes, source completeness and row multiplicity.

- **WINDOWS_AUTH_DATASET** — {"type": "DATASET_IDENTIFIER", "kind": "DATASET", "binding_contract": "Collected Windows Security authentication events with original provider/channel and lossless source-event identity.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **AUTH_TIME** — {"type": "DATETIME", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Source event time, UTC datetime; not collector receipt time.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **AUTH_EVENT_UID** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Collision-free source event key including target/provider/channel/record generation; deduplicate only repeated delivery of the same event. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **AUTH_PROVIDER** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Source provider name. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **AUTH_CHANNEL** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Source channel name. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **AUTH_EVENT_ID** — {"type": "INTEGER", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Parsed numeric Windows EventID.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **AUTH_LOGON_TYPE** — {"type": "INTEGER", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Parsed numeric EventData.LogonType; null for absent/invalid.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **AUTH_TARGET_ENDPOINT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Stable destination endpoint identity valid at event time. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **AUTH_SOURCE_IP** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Canonical client IP; preserve IPv4/IPv6; map absent, dash and unresolved placeholders to null, document gateways/NAT. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **AUTH_TARGET_ACCOUNT_KEY** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Domain-qualified stable target account or SID; not the Subject account. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **AUTH_LOGON_KEY** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Target endpoint boot/LSA epoch plus TargetLogonId; never a Terminal Services session ID without proven mapping. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SESSION_PROCESS_START_DATASET** — {"type": "DATASET_IDENTIFIER", "kind": "DATASET", "binding_contract": "Process-start source with a validated mapping to Windows target logon identity; Cortex Terminal Services ID is not assumed equivalent.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **RDP_PROCESS_TIME** — {"type": "DATETIME", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Process-start time.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **RDP_PROCESS_ENDPOINT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Same stable target endpoint as auth. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **RDP_PROCESS_LOGON_KEY** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Boot/LSA epoch plus Windows logon ID validated against TargetLogonId, or a proven bridge key. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **RDP_PROCESS_INSTANCE** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Unique process instance within endpoint/boot. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **RDP_PROCESS_IMAGE** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Started image basename. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder. Bind lowercase Windows image basename for the explicit powershell.exe/pwsh.exe allowlist.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **RDP_PROCESS_COMMAND** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Original started-process command line. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **RDP_PROCESS_USER** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Observed process principal; keep elevation/user transition context. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **RDP_PROCESS_OPERATION** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Canonical process_start only when the source event is a start, not generic process-category context. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **POWERSHELL_BEHAVIOR_RE2** — {"type": "QUOTED_RE2_STRING", "kind": "VALUE_OR_LITERAL", "binding_contract": "Explicit tested whole-command behavior grammar, e.g. the strict encoded-command grammar in N021. Must include argv0/argument semantics; not a bare PowerShell name or substring. This query uses case_sensitive=true; use tested scoped RE2 case-insensitive groups only for Windows executable/PowerShell keyword tokens that are case-insensitive, retaining argument/path case when significant.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}

[Unfilled bindings file](../bindings/n052.bindings.example.json). All binding values intentionally start as null.

## Pipeline and result semantics

Read the stages in order: the source and early filters establish the population; alter creates explicitly typed aliases; joins/union introduce related evidence; comp/windowcomp/dedup change row cardinality where present; the final projection, sort and limit define displayed rows. A result limit does not bound scanning or join work. Check the identity and time predicates in each subquery against the binding contract.

Estimated cost: HIGH. Bounded lookback and early filters; result caps do not bound upstream scanning. No measured performance claim.

## Limitations and tuning

- Session namespace equivalence is a hard prerequisite. Host/time-only matching must not be substituted.
- The behavior regex is analyst-controlled and requires benign/negative fixtures; user/admin intent and process success remain unproved.
- Repeated or reconnect authentication events can multiply pairs; preserve IDs and validate session lifecycle before any session-count interpretation.
- All bindings are explicit contracts, not claims that a source or field exists. Bind field/expression placeholders to typed tenant fields or tested extraction expressions; never substitute a made-up name.
- One-day telemetry lookback unless stated otherwise. Source-clock, retention, collection and permission gaps can remove evidence; _time filtering must cover each source event time.
- All canonical normalized words in expressions are local mapping contracts, not undocumented Cortex enum values.
- Result limits apply after analysis and do not cap scanned data or join fan-out. No tenant compile, execution, performance or deployment claim.
- ATT&CK, where present, is an analytical interpretation requiring context, not proof of compromise.
- Inventory/coverage/snapshot source bindings must return every relevant validity record under the configured query timeframe; if their storage timestamps differ, establish and test an explicit inner timeframe before use. A missing lookup row is never automatically benign.
- Comparisons are case-sensitive. Binding owners must canonicalize values only in source namespaces proven case-insensitive (such as reviewed Windows service/path/account keys and DNS A-label names), identically on both join sides. Preserve Unix paths, case-sensitive identities, ARN resource components, and source evidence text. All normalized operation/result labels must match the documented local contract spelling exactly.
- Time-difference bounds are expressed at second resolution. Validate subsecond boundary behavior using source fixtures; direct timestamp comparisons enforce before/after ordering where required.

## Acceptance scenarios — NOT RUN

- {"kind": "positive", "fixture": "Auth A on H/session L is followed five minutes later by an encoded PowerShell start on H/L. The source basename is PowerShell.EXE and reviewed command grammar admits its case-equivalent executable token.", "expected": "One linked pair.", "status": "NOT RUN"}
- {"kind": "negative", "fixture": "Same host/time and command behavior occur in a different logon session.", "expected": "Excluded.", "status": "NOT RUN"}
- {"kind": "null", "fixture": "Process has only a Terminal Services ID with no validated Windows logon mapping.", "expected": "Do not bind or run; no linkage result is valid.", "status": "NOT RUN"}
- {"kind": "edge", "fixture": "Qualifying process occurs exactly 1800 seconds later or one second before authentication.", "expected": "1800-second pair included; negative elapsed time excluded.", "status": "NOT RUN"}

These scenarios are plans for representative source fixtures and bounded tenant runs. They have not been executed in Cortex. First inspect the bound schema, then compile the complete body, then exercise positive, benign, negative, null and boundary cases. Do not promote this interactive hunt directly to BIOC/correlation without reviewing that surface.

Independent static review: **PASS**. Full-query C/E: **NOT RUN / NOT RUN**. Static review cannot establish source population or detection effectiveness.

## ATT&CK context

{"id": "T1021.001; T1059.001", "technique": "Remote Desktop Protocol; PowerShell", "tactics": "Lateral Movement (TA0008); Execution (TA0002)"}

Source workbook ATT&CK Enterprise v19.2 hypothesis retained for traceability. The query observes only the behavior in its coverage boundary; mapping is not certified detection coverage.

## References

- [Microsoft event 4624](https://learn.microsoft.com/en-us/previous-versions/windows/it-pro/windows-10/security/threat-protection/auditing/event-4624)
- [Cortex action schema](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/action-actor)
- [Cortex actor schema](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/actor-actor)
- [Cortex XSIAM comp](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/comp?contentId=6TGIbW601nVvh2ycedN_hw)
- [Cortex XSIAM join](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/join?contentId=MyWFbdks6xgkqm_1lNkfGg)
- [Cortex XSIAM bin](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/bin?contentId=CU_u_bTkg_MICr5pUKivwg)
- [Cortex XSIAM sort](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/sort?contentId=a9tB71nqIWhA~Adcae6R7w)
