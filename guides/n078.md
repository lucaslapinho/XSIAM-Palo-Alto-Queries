# N078 — Exact IP Investigation Across Address Roles - Schema Binding Template

Returns exact canonical IP matches across original, translated, DNS-answer and trusted proxy-client roles, preserving source provenance and separate match flags.

Proposal: **OP-006**. Category: **Investigation / Network Activity**.

Artifact: **SCHEMA_BINDING_TEMPLATE**. Product/surface: Cortex XSIAM / Interactive XQL Search. Tenant release, full-query compiler acceptance and execution: **UNVERIFIED / NOT RUN**.

[Query file](../queries/investigation/network-activity/n078-exact-ip-investigation-across-address-roles-schema-binding-template.xql)

## Objective and interpretation

One source observation/address-answer row; a single event can have multiple matching role flags and DNS answers may expand rows.

Output grain: One source observation/address-answer row; a single event can have multiple matching role flags and DNS answers may expand rows.
Timeframe: 1d unless explicitly expanded in body

## Source and field contract

Sources: `["{{IP_OBSERVATION_DATASET}}"]`.

Every schema token must resolve to an inspected field, expression or source with the stated type and meaning. A normalized source contract may require telemetry ingestion, parsing or a separately reviewed view; replacing names alone cannot create missing evidence. Preserve nulls. Verify timezone, identity namespaces, provider/channel, event outcomes, source completeness and row multiplicity.

- **IP_OBSERVATION_DATASET** — {"type": "DATASET_IDENTIFIER", "kind": "DATASET", "binding_contract": "A verified common-schema dataset or view of selected endpoint/firewall/authentication/proxy/DNS observations; preserve source_system/evidence_kind and map only IP roles actually present.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **IP_EVENT_TIME** — {"type": "DATETIME", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Original observation time.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **IP_EVENT_UID** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Source-system-scoped event identifier. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **IP_SOURCE_SYSTEM** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Original dataset/provider provenance. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **IP_EVIDENCE_KIND** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "endpoint_network, firewall, authentication, proxy or dns_response according to actual source. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **IP_ENDPOINT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Verified time-valid endpoint attribution, nullable. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **IP_ACCOUNT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Source-reported principal, nullable. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **IP_SOURCE** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Canonical original/source or local IP, with role retained in source contract. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **IP_DESTINATION** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Canonical original destination/remote IP. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **IP_NAT_SOURCE** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Canonical post-NAT source, null if not applicable. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **IP_NAT_DESTINATION** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Canonical post-NAT destination, null if not applicable. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **IP_DNS_ANSWER** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Actual response answer IP; expand a response array into rows in the verified binding view, preserving event UID. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **IP_PROXY_CLIENT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Trusted forwarded client address only when verified proxy headers/source semantics support it; untrusted forwarded headers remain untrusted context. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **IP_SAFE_DETAIL** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Safe event-specific context without unnecessary credentials. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **IP_TO_REVIEW** — {"type": "QUOTED_IP_STRING", "kind": "VALUE_OR_LITERAL", "binding_contract": "Canonical exact IPv4/IPv6 address to investigate; same normalization as all bound IP fields, never substring/wildcard.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}

[Unfilled bindings file](../bindings/n078.bindings.example.json). All binding values intentionally start as null.

## Pipeline and result semantics

Read the stages in order: the source and early filters establish the population; alter creates explicitly typed aliases; joins/union introduce related evidence; comp/windowcomp/dedup change row cardinality where present; the final projection, sort and limit define displayed rows. A result limit does not bound scanning or join work. Check the identity and time predicates in each subquery against the binding contract.

Estimated cost: MEDIUM. Bounded lookback and early filters; result caps do not bound upstream scanning. No measured performance claim.

## Limitations and tuning

- A common-schema view must be bound and validated per included source; uncollected DNS/proxy branches remain explicit gaps, not fabricated fields.
- Shared, reassigned or translated IPs do not uniquely identify an endpoint/user.
- Do not deduplicate across providers by time/IP; these are independent evidence records.
- Canonical IPv4-mapped IPv6 handling must be deliberate and identical for the parameter and each field.
- All bindings are explicit contracts, not claims that a source or field exists. Bind field/expression placeholders to typed tenant fields or tested extraction expressions; never substitute a made-up name.
- One-day telemetry lookback unless stated otherwise. Source-clock, retention, collection and permission gaps can remove evidence; _time filtering must cover each source event time.
- All canonical normalized words in expressions are local mapping contracts, not undocumented Cortex enum values.
- Result limits apply after analysis and do not cap scanned data or join fan-out. No tenant compile, execution, performance or deployment claim.
- ATT&CK, where present, is an analytical interpretation requiring context, not proof of compromise.
- Inventory/coverage/snapshot source bindings must return every relevant validity record under the configured query timeframe; if their storage timestamps differ, establish and test an explicit inner timeframe before use. A missing lookup row is never automatically benign.
- Comparisons are case-sensitive. Binding owners must canonicalize values only in source namespaces proven case-insensitive (such as reviewed Windows service/path/account keys and DNS A-label names), identically on both join sides. Preserve Unix paths, case-sensitive identities, ARN resource components, and source evidence text. All normalized operation/result labels must match the documented local contract spelling exactly.
- Time-difference bounds are expressed at second resolution. Validate subsecond boundary behavior using source fixtures; direct timestamp comparisons enforce before/after ordering where required.

## Acceptance scenarios — NOT RUN

- {"kind": "positive", "fixture": "The target IP appears only in post-NAT destination and DNS-answer roles of separate events.", "expected": "Both returned with the correct flags.", "status": "NOT RUN"}
- {"kind": "negative", "fixture": "The target string appears only inside a command or a longer IP-like string.", "expected": "Excluded.", "status": "NOT RUN"}
- {"kind": "null", "fixture": "All IP fields are null.", "expected": "Excluded without assigning UNKNOWN to the target.", "status": "NOT RUN"}
- {"kind": "edge", "fixture": "The same address is both source and destination in loopback or hairpin traffic.", "expected": "Both flags set; no arbitrary single-role choice.", "status": "NOT RUN"}

These scenarios are plans for representative source fixtures and bounded tenant runs. They have not been executed in Cortex. First inspect the bound schema, then compile the complete body, then exercise positive, benign, negative, null and boundary cases. Do not promote this interactive hunt directly to BIOC/correlation without reviewing that surface.

Independent static review: **PASS**. Full-query C/E: **NOT RUN / NOT RUN**. Static review cannot establish source population or detection effectiveness.

## ATT&CK context

N/A — operational investigation

Operational visibility does not imply adversary behavior.

## References

- [Cortex action schema](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/action-actor)
- [PAN-OS traffic log fields](https://docs.paloaltonetworks.com/ngfw/administration/monitoring/use-syslog-for-monitoring/syslog-field-descriptions/traffic-log-fields)
- [Microsoft event 4624](https://learn.microsoft.com/en-us/previous-versions/windows/it-pro/windows-10/security/threat-protection/auditing/event-4624)
- [Cortex XSIAM comp](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/comp?contentId=6TGIbW601nVvh2ycedN_hw)
- [Cortex XSIAM join](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/join?contentId=MyWFbdks6xgkqm_1lNkfGg)
- [Cortex XSIAM bin](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/bin?contentId=CU_u_bTkg_MICr5pUKivwg)
- [Cortex XSIAM sort](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/sort?contentId=a9tB71nqIWhA~Adcae6R7w)
