# N043 — Threat Hunting - No-preauthentication Accounts and TGT Request Activity

Correlates successful no-preauthentication TGT requests with time-valid account configuration, source/request baselines and approved service exceptions.

Proposal: **TH-018**. Category: **Threat Hunting / Credential Access**.

Artifact: **SCHEMA_BINDING_TEMPLATE**. Product/surface: Cortex XSIAM / Interactive XQL Search. Tenant release, full-query compiler acceptance and execution: **UNVERIFIED / NOT RUN**.

[Query file](../queries/threat-hunting/credential-access/n043-threat-hunting-no-preauthentication-accounts-and-tgt-request-activity.xql)

## Objective and interpretation

Full account-state-plus-activity template; account flags and successful request semantics are independent mandatory evidence.

Output grain: See the query projection and aggregation.
Timeframe: 1d

## Source and field contract

Sources: `["{{ASREP_REQUEST_BASELINE_DATASET}}", "{{PREAUTH_ACCOUNT_STATE_DATASET}}", "{{TGT_REQUESTS_DATASET}}"]`.

Every schema token must resolve to an inspected field, expression or source with the stated type and meaning. A normalized source contract may require telemetry ingestion, parsing or a separately reviewed view; replacing names alone cannot create missing evidence. Preserve nulls. Verify timezone, identity namespaces, provider/channel, event outcomes, source completeness and row multiplicity.

- **TGT_REQUESTS_DATASET** — {"type": "XQL dataset identifier", "description": "Actual read-only source for TGT_REQUESTS. Bind a documented/inspected dataset or reviewed view; this name is not a claim of an existing integration.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **TGT_REQUESTS_EVENT_TIME** — {"type": "DATETIME scalar expression", "description": "4768 request time Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **TGT_REQUESTS_EVENT_ID** — {"type": "INTEGER scalar expression", "description": "Provider-verified event ID Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **TGT_REQUESTS_ACCOUNT_SID** — {"type": "STRING scalar expression", "description": "Resolved target account SID Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **TGT_REQUESTS_DOMAIN_ID** — {"type": "STRING scalar expression", "description": "Authoritative domain identity Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **TGT_REQUESTS_SOURCE_IP** — {"type": "STRING scalar expression", "description": "Observed client address Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **TGT_REQUESTS_PREAUTH_TYPE** — {"type": "INTEGER scalar expression", "description": "Parsed PreAuthType with the event-version meaning Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **TGT_REQUESTS_SUCCESS** — {"type": "BOOLEAN scalar expression", "description": "Successful TGT issue outcome Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **TGT_REQUESTS_ENCRYPTION_TYPE** — {"type": "STRING scalar expression", "description": "Original ticket encryption value Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **PREAUTH_ACCOUNT_STATE_DATASET** — {"type": "XQL dataset identifier", "description": "Actual read-only source for PREAUTH_ACCOUNT_STATE. Bind a documented/inspected dataset or reviewed view; this name is not a claim of an existing integration.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **PREAUTH_ACCOUNT_STATE_A_SID** — {"type": "STRING scalar expression", "description": "Same account SID Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **PREAUTH_ACCOUNT_STATE_A_DOMAIN** — {"type": "STRING scalar expression", "description": "Same domain identity Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **PREAUTH_ACCOUNT_STATE_DOES_NOT_REQUIRE_PREAUTH** — {"type": "BOOLEAN scalar expression", "description": "Verified effective DONT_REQ_PREAUTH configuration, not failed-auth inference Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **PREAUTH_ACCOUNT_STATE_EFFECTIVE_FROM** — {"type": "DATETIME scalar expression", "description": "Start of configuration validity Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **PREAUTH_ACCOUNT_STATE_EFFECTIVE_UNTIL** — {"type": "DATETIME scalar expression", "description": "End of configuration validity; use explicit future bound for currently open state Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **PREAUTH_ACCOUNT_STATE_APPROVED_EXCEPTION** — {"type": "BOOLEAN scalar expression", "description": "Current approved non-preauthentication service exception; null means unassessed Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **ASREP_REQUEST_BASELINE_DATASET** — {"type": "XQL dataset identifier", "description": "Actual read-only source for ASREP_REQUEST_BASELINE. Bind a documented/inspected dataset or reviewed view; this name is not a claim of an existing integration.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **ASREP_REQUEST_BASELINE_B_SID** — {"type": "STRING scalar expression", "description": "Same account SID; exactly one frozen baseline row per SID/domain/source key, including explicit zero rows when source absence is established Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **ASREP_REQUEST_BASELINE_B_DOMAIN** — {"type": "STRING scalar expression", "description": "Same domain identity Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **ASREP_REQUEST_BASELINE_KNOWN_SOURCE** — {"type": "STRING scalar expression", "description": "Previously expected source address Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **ASREP_REQUEST_BASELINE_PRIOR_REQUESTS** — {"type": "INTEGER scalar expression", "description": "Prior covered successful no-preauth requests from this account/source Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **ASREP_REQUEST_BASELINE_EXPECTED_DAILY_PEAK** — {"type": "INTEGER scalar expression", "description": "Reviewed expected daily request count Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **ASREP_REQUEST_BASELINE_BASELINE_COMPLETE** — {"type": "BOOLEAN scalar expression", "description": "Prior baseline coverage validated. Freeze a comparable snapshot ending at or before evaluation_time minus 86400 seconds, exclude all rolling-current-window observations, and require one row per SID/domain/source key Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **ASREP_DAILY_THRESHOLD** — {"type": "INTEGER literal", "description": "Positive daily request triage floor chosen after environment baseline review", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}

[Unfilled bindings file](../bindings/n043.bindings.example.json). All binding values intentionally start as null.

## Pipeline and result semantics

Read the stages in order: the source and early filters establish the population; alter creates explicitly typed aliases; joins/union introduce related evidence; comp/windowcomp/dedup change row cardinality where present; the final projection, sort and limit define displayed rows. A result limit does not bound scanning or join work. Check the identity and time predicates in each subquery against the binding contract.

Estimated cost: MEDIUM. Bounded lookback and early filters; result caps do not bound upstream scanning. No measured performance claim.

## Limitations and tuning

- Historical creation flags do not establish current preauthentication state; validity intervals must not overlap for a SID/domain.
- Unusual request activity is not proof that an AS-REP was cracked. Missing baseline yields UNASSESSED.
- No tenant compilation or execution. Validate nulls, access, retention and source collection before interpreting empty results.
- A terminal limit bounds displayed output, not upstream scans or join multiplicity.

## Acceptance scenarios — NOT RUN

- {"kind": "positive", "fixture": "Account has a valid no-preauth flag and successful PreAuthType 0 requests exceed its covered baseline", "expected": "Request-burst candidate with configuration evidence."}
- {"kind": "negative", "fixture": "Only 4771 failures, preauth-enabled account or PreAuthType not zero", "expected": "Excluded."}
- {"kind": "null", "fixture": "Account flag or SID unavailable", "expected": "No fabricated exposure correlation; record the coverage gap."}
- {"kind": "edge", "fixture": "Flag was disabled after the request or two account-state intervals overlap", "expected": "Only the historically applicable unique interval is allowed; overlapping input fails acceptance."}

These scenarios are plans for representative source fixtures and bounded tenant runs. They have not been executed in Cortex. First inspect the bound schema, then compile the complete body, then exercise positive, benign, negative, null and boundary cases. Do not promote this interactive hunt directly to BIOC/correlation without reviewing that surface.

Independent static review: **PASS**. Full-query C/E: **NOT RUN / NOT RUN**. Static review cannot establish source population or detection effectiveness.

## ATT&CK context

{"id": "T1558.004", "technique": "AS-REP Roasting", "tactics": "Credential Access (TA0006)"}

Source workbook ATT&CK Enterprise v19.2 hypothesis retained for traceability. The query observes only the behavior in its coverage boundary; mapping is not certified detection coverage.

## References

- [Official source for TH-018](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields)
- [Official source for TH-018](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/action-actor)
- [Official source for TH-018](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/actor-actor)
- [Official source for TH-018](https://cortex-docs.paloaltonetworks.com/xql-command-reference-guide/readme/functions/timestamp_diff)
- [Official source for TH-018](https://docs-cortex.paloaltonetworks.com/r/Cortex/Cortex-XQL-Command-Reference/Example-2-Left-join?contentId=5iT66RH4ney5McWya7JKjQ)
- [Official source for TH-018](https://learn.microsoft.com/en-us/previous-versions/windows/it-pro/windows-10/security/threat-protection/auditing/event-4768)
- [Official source for TH-018](https://learn.microsoft.com/en-us/troubleshoot/windows-server/active-directory/useraccountcontrol-manipulate-account-properties)
- [Official source for TH-018](https://attack.mitre.org/techniques/T1558/004/)
