# N038 — Threat Hunting - LSASS Memory-capable Access and Dump-file Candidates

Reviews non-baselined or unassessed processes with observed LSASS memory-capable access and correlates subsequent dump-file candidates by source process and endpoint within ten minutes.

Proposal: **TH-013**. Category: **Threat Hunting / Credential Access**.

Artifact: **SCHEMA_BINDING_TEMPLATE**. Product/surface: Cortex XSIAM / Interactive XQL Search. Tenant release, full-query compiler acceptance and execution: **UNVERIFIED / NOT RUN**.

[Query file](../queries/threat-hunting/credential-access/n038-threat-hunting-lsass-memory-capable-access-and-dump-file-candidates.xql)

## Objective and interpretation

Access-right evidence is mandatory; source/target process names alone cannot satisfy the query. Dump-file kind requires artifact/operation evidence and remains a candidate unless contents are validated.

Output grain: See the query projection and aggregation.
Timeframe: 1d

## Source and field contract

Sources: `["{{ACCESS_FOLLOWON_FILES_DATASET}}", "{{LSASS_ACCESS_DATASET}}"]`.

Every schema token must resolve to an inspected field, expression or source with the stated type and meaning. A normalized source contract may require telemetry ingestion, parsing or a separately reviewed view; replacing names alone cannot create missing evidence. Preserve nulls. Verify timezone, identity namespaces, provider/channel, event outcomes, source completeness and row multiplicity.

- **LSASS_ACCESS_DATASET** — {"type": "XQL dataset identifier", "description": "Actual read-only source for LSASS_ACCESS. Bind a documented/inspected dataset or reviewed view; this name is not a claim of an existing integration.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **LSASS_ACCESS_ANCHOR_TIME** — {"type": "DATETIME scalar expression", "description": "Target process access time Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **LSASS_ACCESS_ENDPOINT_ID** — {"type": "STRING scalar expression", "description": "Stable endpoint identifier Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **LSASS_ACCESS_ANCHOR_PROCESS_ID** — {"type": "STRING scalar expression", "description": "Source accessing process instance Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **LSASS_ACCESS_ANCHOR_COMMAND** — {"type": "STRING scalar expression", "description": "Accessing process image/path/command context Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **LSASS_ACCESS_ANCHOR_ACTOR** — {"type": "STRING scalar expression", "description": "Accessing account Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **LSASS_ACCESS_TARGET_IMAGE** — {"type": "STRING scalar expression", "description": "Verified target process basename Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **LSASS_ACCESS_TARGET_PROCESS_ID** — {"type": "STRING scalar expression", "description": "Target LSASS process instance Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **LSASS_ACCESS_ACCESS_RIGHTS** — {"type": "STRING scalar expression", "description": "Actual requested/granted access mask or equivalent sensor signal Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **LSASS_ACCESS_MEMORY_READ_CAPABLE** — {"type": "BOOLEAN scalar expression", "description": "Granted PROCESS_VM_READ capability or documented equivalent memory-access signal; distinguish requested from granted and do not label a handle-open as completed dumping Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **LSASS_ACCESS_ACCESS_RESULT** — {"type": "STRING scalar expression", "description": "Actual access outcome Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **LSASS_ACCESS_EXPECTED_ACCESSOR** — {"type": "BOOLEAN scalar expression", "description": "Scoped approved security/diagnostic accessor baseline match; null when unassessed Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **ACCESS_FOLLOWON_FILES_DATASET** — {"type": "XQL dataset identifier", "description": "Actual read-only source for ACCESS_FOLLOWON_FILES. Bind a documented/inspected dataset or reviewed view; this name is not a claim of an existing integration.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **ACCESS_FOLLOWON_FILES_CTX_HOST** — {"type": "STRING scalar expression", "description": "Stable endpoint ID in the same namespace as the anchor Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **ACCESS_FOLLOWON_FILES_CTX_ROOT** — {"type": "STRING scalar expression", "description": "Root process instance for this bounded ancestry relation. The root must be the source process that accessed LSASS; descendant widening requires a separately verified relation. A native actor or causality-owner field may only be used if that exact relationship is verified; otherwise bind a reviewed ancestry bridge/view. Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **ACCESS_FOLLOWON_FILES_CTX_TIME** — {"type": "DATETIME scalar expression", "description": "Original observed activity time Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **ACCESS_FOLLOWON_FILES_CTX_KIND** — {"type": "STRING scalar expression", "description": "Explicit normalized activity kind derived from verified event type/subtype, not a name-only inference Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **ACCESS_FOLLOWON_FILES_CTX_PROCESS** — {"type": "STRING scalar expression", "description": "Process instance actually producing the activity Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **ACCESS_FOLLOWON_FILES_CTX_TEXT** — {"type": "STRING scalar expression", "description": "Activity detail: actual command, downloaded object/URL or file path as appropriate; retain the original event reference Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **ACCESS_FOLLOWON_FILES_CTX_OUTCOME** — {"type": "STRING scalar expression", "description": "Actual operation outcome; UNKNOWN when absent, never success by default Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}

[Unfilled bindings file](../bindings/n038.bindings.example.json). All binding values intentionally start as null.

## Pipeline and result semantics

Read the stages in order: the source and early filters establish the population; alter creates explicitly typed aliases; joins/union introduce related evidence; comp/windowcomp/dedup change row cardinality where present; the final projection, sort and limit define displayed rows. A result limit does not bound scanning or join work. Check the identity and time predicates in each subquery against the binding contract.

Estimated cost: MEDIUM. Bounded lookback and early filters; result caps do not bound upstream scanning. No measured performance claim.

## Limitations and tuning

- ProcessAccess/handle capability does not prove that memory was read or dumped. Security tools can perform the same access.
- The source must carry rights, target identity and outcome. Suppressing approved accessors requires a reviewed complete baseline; unknown accessors remain visible.
- No tenant compilation or execution. Validate nulls, access, retention and source collection before interpreting empty results.
- A terminal limit bounds displayed output, not upstream scans or join multiplicity.

## Acceptance scenarios — NOT RUN

- {"kind": "positive", "fixture": "Unapproved source receives memory-read-capable LSASS access, then writes a dump candidate 15 seconds later", "expected": "Access anchor and subsequent dump row."}
- {"kind": "negative", "fixture": "Only query-information rights or source targets a different process", "expected": "Excluded."}
- {"kind": "null", "fixture": "Approval baseline status missing, but access target/capability verified", "expected": "Retained as unassessed access."}
- {"kind": "edge", "fixture": "Dump on another host or before access", "expected": "No follow-on association."}

These scenarios are plans for representative source fixtures and bounded tenant runs. They have not been executed in Cortex. First inspect the bound schema, then compile the complete body, then exercise positive, benign, negative, null and boundary cases. Do not promote this interactive hunt directly to BIOC/correlation without reviewing that surface.

Independent static review: **PASS**. Full-query C/E: **NOT RUN / NOT RUN**. Static review cannot establish source population or detection effectiveness.

## ATT&CK context

{"id": "T1003.001", "technique": "LSASS Memory", "tactics": "Credential Access (TA0006)"}

Source workbook ATT&CK Enterprise v19.2 hypothesis retained for traceability. The query observes only the behavior in its coverage boundary; mapping is not certified detection coverage.

## References

- [Official source for TH-013](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields)
- [Official source for TH-013](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/action-actor)
- [Official source for TH-013](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/actor-actor)
- [Official source for TH-013](https://cortex-docs.paloaltonetworks.com/xql-command-reference-guide/readme/functions/timestamp_diff)
- [Official source for TH-013](https://docs-cortex.paloaltonetworks.com/r/Cortex/Cortex-XQL-Command-Reference/Example-2-Left-join?contentId=5iT66RH4ney5McWya7JKjQ)
- [Official source for TH-013](https://docs-cortex.paloaltonetworks.com/r/Cortex/Cortex-XQL-Command-Reference/Example-2-Union-with-an-Inner-XQL-Query)
- [Official source for TH-013](https://learn.microsoft.com/en-us/sysinternals/downloads/sysmon)
- [Official source for TH-013](https://learn.microsoft.com/en-us/windows/win32/procthread/process-security-and-access-rights)
- [Official source for TH-013](https://attack.mitre.org/techniques/T1003/001/)
