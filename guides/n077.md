# N077 — Classified RDP Traffic on Standard and Nonstandard Ports - Schema Binding Template

Shows RDP-classified network observations on any valid destination port and attaches client-process context only through a verified stable process-instance relationship.

Proposal: **OP-004**. Category: **Investigation / RDP Network Activity**.

Artifact: **SCHEMA_BINDING_TEMPLATE**. Product/surface: Cortex XSIAM / Interactive XQL Search. Tenant release, full-query compiler acceptance and execution: **UNVERIFIED / NOT RUN**.

[Query file](../queries/investigation/rdp-network-activity/n077-classified-rdp-traffic-on-standard-and-nonstandard-ports-schema-binding-template.xql)

## Objective and interpretation

One RDP-classified network record, optionally associated with a process start; includes standard and nonstandard ports.

Output grain: One RDP-classified network record, optionally associated with a process start; includes standard and nonstandard ports.
Timeframe: 1d unless explicitly expanded in body

## Source and field contract

Sources: `["{{RDP_NETWORK_CLASSIFICATION_DATASET}}"]`.

Every schema token must resolve to an inspected field, expression or source with the stated type and meaning. A normalized source contract may require telemetry ingestion, parsing or a separately reviewed view; replacing names alone cannot create missing evidence. Preserve nulls. Verify timezone, identity namespaces, provider/channel, event outcomes, source completeness and row multiplicity.

- **RDP_NETWORK_CLASSIFICATION_DATASET** — {"type": "DATASET_IDENTIFIER", "kind": "DATASET", "binding_contract": "Application-classified traffic with endpoint-observed canonical tuples; classification must come from verified App-ID/protocol telemetry, not a port guess.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **RDP_FLOW_TIME** — {"type": "DATETIME", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Network observation time.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **RDP_FLOW_UID** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Unique network evidence record; no universal unique-session assumption. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **RDP_CLIENT_ENDPOINT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Time-valid client endpoint attribution or null. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **RDP_FLOW_SOURCE_IP** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Canonical client address at the endpoint sensor boundary; translation must be validated. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **RDP_FLOW_SOURCE_PORT** — {"type": "INTEGER", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Client source port at the same sensor boundary.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **RDP_FLOW_DEST_IP** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Server address at that boundary. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **RDP_FLOW_DEST_PORT** — {"type": "INTEGER", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Observed server destination port.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **RDP_FLOW_PROTOCOL** — {"type": "INTEGER", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Canonical IP protocol number after typed source normalization.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **RDP_FLOW_APPLICATION** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Actual application classification. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **RDP_FLOW_ACTION** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Original network/firewall outcome classification, not authentication. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **RDP_FLOW_PROCESS_INSTANCE** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Network-to-initiating-process instance only if source or exact validated correlation provides it; otherwise null. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **RDP_APPLICATION_VALUES** — {"type": "QUOTED_STRING_LIST", "kind": "VALUE_OR_LITERAL", "binding_contract": "Verified RDP application identifiers from the selected classifier/version; no invented App-ID spelling.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}

[Unfilled bindings file](../bindings/n077.bindings.example.json). All binding values intentionally start as null.

## Pipeline and result semantics

Read the stages in order: the source and early filters establish the population; alter creates explicitly typed aliases; joins/union introduce related evidence; comp/windowcomp/dedup change row cardinality where present; the final projection, sort and limit define displayed rows. A result limit does not bound scanning or join work. Check the identity and time predicates in each subquery against the binding contract.

Estimated cost: HIGH. Bounded lookback and early filters; result caps do not bound upstream scanning. No measured performance claim.

## Limitations and tuning

- Client starts without traffic are outside this traffic-driven view and remain available through process timelines.
- Application classification is not authentication success. Alternative or renamed clients can still appear when network classification is available.
- Do not invent a firewall process-instance field or correlate by host/time alone; missing linkage retains network evidence with null process context.
- NAT translation and sensor-boundary mapping are required before claiming endpoint identity.
- All bindings are explicit contracts, not claims that a source or field exists. Bind field/expression placeholders to typed tenant fields or tested extraction expressions; never substitute a made-up name.
- One-day telemetry lookback unless stated otherwise. Source-clock, retention, collection and permission gaps can remove evidence; _time filtering must cover each source event time.
- All canonical normalized words in expressions are local mapping contracts, not undocumented Cortex enum values.
- Result limits apply after analysis and do not cap scanned data or join fan-out. No tenant compile, execution, performance or deployment claim.
- ATT&CK, where present, is an analytical interpretation requiring context, not proof of compromise.
- Inventory/coverage/snapshot source bindings must return every relevant validity record under the configured query timeframe; if their storage timestamps differ, establish and test an explicit inner timeframe before use. A missing lookup row is never automatically benign.
- Comparisons are case-sensitive. Binding owners must canonicalize values only in source namespaces proven case-insensitive (such as reviewed Windows service/path/account keys and DNS A-label names), identically on both join sides. Preserve Unix paths, case-sensitive identities, ARN resource components, and source evidence text. All normalized operation/result labels must match the documented local contract spelling exactly.
- Time-difference bounds are expressed at second resolution. Validate subsecond boundary behavior using source fixtures; direct timestamp comparisons enforce before/after ordering where required.

## Acceptance scenarios — NOT RUN

- {"kind": "positive", "fixture": "Classifier identifies RDP on TCP destination 4444 and a verified process instance matches its client start.", "expected": "NONSTANDARD_PORT with client context.", "status": "NOT RUN"}
- {"kind": "negative", "fixture": "Traffic on 3389 is classified as another protocol, or only mstsc starts without traffic.", "expected": "Excluded from this classified-traffic view.", "status": "NOT RUN"}
- {"kind": "null", "fixture": "RDP traffic has no verified process-instance relation.", "expected": "Network record remains with null client process fields.", "status": "NOT RUN"}
- {"kind": "edge", "fixture": "A client process started before the one-day window but traffic occurs inside it.", "expected": "Network record remains; missing process-start context is not proof of an unknown client.", "status": "NOT RUN"}

These scenarios are plans for representative source fixtures and bounded tenant runs. They have not been executed in Cortex. First inspect the bound schema, then compile the complete body, then exercise positive, benign, negative, null and boundary cases. Do not promote this interactive hunt directly to BIOC/correlation without reviewing that surface.

Independent static review: **PASS**. Full-query C/E: **NOT RUN / NOT RUN**. Static review cannot establish source population or detection effectiveness.

## ATT&CK context

N/A — operational investigation

Operational visibility does not imply adversary behavior.

## References

- [PAN-OS traffic log fields](https://docs.paloaltonetworks.com/ngfw/administration/monitoring/use-syslog-for-monitoring/syslog-field-descriptions/traffic-log-fields)
- [Cortex general event schema](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields)
- [Cortex action schema](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/action-actor)
- [Cortex XSIAM comp](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/comp?contentId=6TGIbW601nVvh2ycedN_hw)
- [Cortex XSIAM join](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/join?contentId=MyWFbdks6xgkqm_1lNkfGg)
- [Cortex XSIAM bin](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/bin?contentId=CU_u_bTkg_MICr5pUKivwg)
- [Cortex XSIAM sort](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/sort?contentId=a9tB71nqIWhA~Adcae6R7w)
- [Official XSIAM process-start example](https://cortex-docs.paloaltonetworks.com/cortex-xsiam/detect-investigate-and-respond-to-threats/threat-management/detection-rules/what-are-detection-rules/whats-a-bioc/create-a-bioc-rule)
