# N037 — Threat Hunting - Configured SSH Authorized Keys File Changes

Correlates successful mutations of configured SSH authorized-key files with owning accounts, writer/process identity and approved key-change windows. Paths remain case-sensitive.

Proposal: **TH-012**. Category: **Threat Hunting / Persistence**.

Artifact: **SCHEMA_BINDING_TEMPLATE**. Product/surface: Cortex XSIAM / Interactive XQL Search. Tenant release, full-query compiler acceptance and execution: **UNVERIFIED / NOT RUN**.

[Query file](../queries/threat-hunting/persistence/n037-threat-hunting-configured-ssh-authorized-keys-file-changes.xql)

## Objective and interpretation

Complete account/writer/change-window template including configured nondefault paths and atomic replacement; does not infer changes from accepted public-key logons.

Output grain: See the query projection and aggregation.
Timeframe: 1d

## Source and field contract

Sources: `["{{AUTHORIZED_KEYS_CONFIGURATION_DATASET}}", "{{KEY_CHANGE_WINDOWS_DATASET}}", "{{SSH_KEY_FILE_CHANGES_DATASET}}"]`.

Every schema token must resolve to an inspected field, expression or source with the stated type and meaning. A normalized source contract may require telemetry ingestion, parsing or a separately reviewed view; replacing names alone cannot create missing evidence. Preserve nulls. Verify timezone, identity namespaces, provider/channel, event outcomes, source completeness and row multiplicity.

- **SSH_KEY_FILE_CHANGES_DATASET** — {"type": "XQL dataset identifier", "description": "Actual read-only source for SSH_KEY_FILE_CHANGES. Bind a documented/inspected dataset or reviewed view; this name is not a claim of an existing integration.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **SSH_KEY_FILE_CHANGES_EVENT_TIME** — {"type": "DATETIME scalar expression", "description": "Linux file mutation time Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **SSH_KEY_FILE_CHANGES_HOST_ID** — {"type": "STRING scalar expression", "description": "Stable Linux endpoint identity Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **SSH_KEY_FILE_CHANGES_RECORD_ID** — {"type": "STRING scalar expression", "description": "Unique mutation record key Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **SSH_KEY_FILE_CHANGES_FILE_PATH** — {"type": "STRING scalar expression", "description": "Case-preserving resolved canonical file path, including rename destination Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **SSH_KEY_FILE_CHANGES_OPERATION** — {"type": "STRING scalar expression", "description": "CREATE, WRITE, RENAME or DELETE; never map reads to changes Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **SSH_KEY_FILE_CHANGES_SUCCESSFUL** — {"type": "BOOLEAN scalar expression", "description": "Actual mutation success Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **SSH_KEY_FILE_CHANGES_WRITER_ID** — {"type": "STRING scalar expression", "description": "Writer account identity Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **SSH_KEY_FILE_CHANGES_WRITER_PROCESS** — {"type": "STRING scalar expression", "description": "Writer process instance Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **AUTHORIZED_KEYS_CONFIGURATION_DATASET** — {"type": "XQL dataset identifier", "description": "Actual read-only source for AUTHORIZED_KEYS_CONFIGURATION. Bind a documented/inspected dataset or reviewed view; this name is not a claim of an existing integration.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **AUTHORIZED_KEYS_CONFIGURATION_KEY_HOST** — {"type": "STRING scalar expression", "description": "Stable endpoint identity Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **AUTHORIZED_KEYS_CONFIGURATION_KEY_PATH** — {"type": "STRING scalar expression", "description": "Actual configured AuthorizedKeysFile path after token/user/symlink resolution; case-preserving Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **AUTHORIZED_KEYS_CONFIGURATION_ACCOUNT_ID** — {"type": "STRING scalar expression", "description": "Account for which this key file is configured Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **AUTHORIZED_KEYS_CONFIGURATION_CONFIGURATION_FROM** — {"type": "DATETIME scalar expression", "description": "Verified effective configuration start inclusive, not ingestion time Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **AUTHORIZED_KEYS_CONFIGURATION_CONFIGURATION_UNTIL** — {"type": "DATETIME scalar expression", "description": "Verified effective configuration end exclusive; null only for a currently open interval. Intervals per host/path/account must not overlap Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **KEY_CHANGE_WINDOWS_DATASET** — {"type": "XQL dataset identifier", "description": "Actual read-only source for KEY_CHANGE_WINDOWS. Bind a documented/inspected dataset or reviewed view; this name is not a claim of an existing integration.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **KEY_CHANGE_WINDOWS_W_HOST** — {"type": "STRING scalar expression", "description": "Stable endpoint identity Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **KEY_CHANGE_WINDOWS_W_ACCOUNT** — {"type": "STRING scalar expression", "description": "Owner/account identity Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **KEY_CHANGE_WINDOWS_WINDOW_START** — {"type": "DATETIME scalar expression", "description": "Approved provisioning/rotation window start Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **KEY_CHANGE_WINDOWS_WINDOW_END** — {"type": "DATETIME scalar expression", "description": "Approved window end Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **KEY_CHANGE_WINDOWS_CHANGE_ID** — {"type": "STRING scalar expression", "description": "Actual change reference Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}

[Unfilled bindings file](../bindings/n037.bindings.example.json). All binding values intentionally start as null.

## Pipeline and result semantics

Read the stages in order: the source and early filters establish the population; alter creates explicitly typed aliases; joins/union introduce related evidence; comp/windowcomp/dedup change row cardinality where present; the final projection, sort and limit define displayed rows. A result limit does not bound scanning or join work. Check the identity and time predicates in each subquery against the binding contract.

Estimated cost: MEDIUM. Bounded lookback and early filters; result caps do not bound upstream scanning. No measured performance claim.

## Limitations and tuning

- The path inventory must resolve actual AuthorizedKeysFile settings and symlink/rename behavior at event time.
- Window overlap is not proof that a particular writer or key was approved. No key contents or secrets are required in this output.
- No tenant compilation or execution. Validate nulls, access, retention and source collection before interpreting empty results.
- A terminal limit bounds displayed output, not upstream scans or join multiplicity.

## Acceptance scenarios — NOT RUN

- {"kind": "positive", "fixture": "Successful atomic rename replaces the configured key file during an approved account rotation window", "expected": "Mutation, account, writer and overlapping change reference appear."}
- {"kind": "negative", "fixture": "Read-only access or similarly named unconfigured file", "expected": "Excluded."}
- {"kind": "null", "fixture": "No approved window exists", "expected": "Mutation retained with NO_APPROVED_WINDOW_MATCH."}
- {"kind": "edge", "fixture": "Two Linux paths differ only in letter case", "expected": "Only the exact configured path matches."}

These scenarios are plans for representative source fixtures and bounded tenant runs. They have not been executed in Cortex. First inspect the bound schema, then compile the complete body, then exercise positive, benign, negative, null and boundary cases. Do not promote this interactive hunt directly to BIOC/correlation without reviewing that surface.

Independent static review: **PASS**. Full-query C/E: **NOT RUN / NOT RUN**. Static review cannot establish source population or detection effectiveness.

## ATT&CK context

{"id": "T1098.004", "technique": "SSH Authorized Keys", "tactics": "Persistence (TA0003)"}

Source workbook ATT&CK Enterprise v19.2 hypothesis retained for traceability. The query observes only the behavior in its coverage boundary; mapping is not certified detection coverage.

## References

- [Official source for TH-012](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields)
- [Official source for TH-012](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/action-actor)
- [Official source for TH-012](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/actor-actor)
- [Official source for TH-012](https://cortex-docs.paloaltonetworks.com/xql-command-reference-guide/readme/functions/timestamp_diff)
- [Official source for TH-012](https://docs-cortex.paloaltonetworks.com/r/Cortex/Cortex-XQL-Command-Reference/Example-2-Left-join?contentId=5iT66RH4ney5McWya7JKjQ)
- [Official source for TH-012](https://man.openbsd.org/sshd_config.5)
- [Official source for TH-012](https://attack.mitre.org/techniques/T1098/004/)
