# N031 — Threat Hunting - Scheduled Task Configuration Change Review

Reviews task creation and modification configurations against the current approved configuration snapshot, preserving subject, XML author, actions, triggers, principal and run level.

Proposal: **TH-006**. Category: **Threat Hunting / Persistence**.

Artifact: **SCHEMA_BINDING_TEMPLATE**. Product/surface: Cortex XSIAM / Interactive XQL Search. Tenant release, full-query compiler acceptance and execution: **UNVERIFIED / NOT RUN**.

[Query file](../queries/threat-hunting/persistence/n031-threat-hunting-scheduled-task-configuration-change-review.xql)

## Objective and interpretation

Actual task-audit configuration workflow reviewed against current approval state. It does not establish whether approval existed at the historical event time. A missing match is a review state, not unauthorized execution.

Output grain: See the query projection and aggregation.
Timeframe: 1d

## Source and field contract

Sources: `["{{APPROVED_TASKS_DATASET}}", "{{TASK_AUDIT_DATASET}}"]`.

Every schema token must resolve to an inspected field, expression or source with the stated type and meaning. A normalized source contract may require telemetry ingestion, parsing or a separately reviewed view; replacing names alone cannot create missing evidence. Preserve nulls. Verify timezone, identity namespaces, provider/channel, event outcomes, source completeness and row multiplicity.

- **TASK_AUDIT_DATASET** — {"type": "XQL dataset identifier", "description": "Actual read-only source for TASK_AUDIT. Bind a documented/inspected dataset or reviewed view; this name is not a claim of an existing integration.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **TASK_AUDIT_TASK_TIME** — {"type": "DATETIME scalar expression", "description": "Creation/modification event time Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **TASK_AUDIT_HOST_ID** — {"type": "STRING scalar expression", "description": "Stable host identifier Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **TASK_AUDIT_RECORD_KEY** — {"type": "STRING scalar expression", "description": "Stable event key including provider/host/record identity Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **TASK_AUDIT_EVENT_ID** — {"type": "INTEGER scalar expression", "description": "Windows audit ID Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **TASK_AUDIT_TASK_NAME** — {"type": "STRING scalar expression", "description": "Configured task name Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **TASK_AUDIT_CREATOR_SID** — {"type": "STRING scalar expression", "description": "Audited subject SID; distinct from XML author Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **TASK_AUDIT_XML_AUTHOR** — {"type": "STRING scalar expression", "description": "Author extracted from the actual task XML Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **TASK_AUDIT_ACTION_COMMAND** — {"type": "STRING scalar expression", "description": "Configured executable and arguments from all actions, serialized without losing additional actions Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **TASK_AUDIT_TASK_TRIGGERS** — {"type": "STRING scalar expression", "description": "Configured triggers from the actual XML Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **TASK_AUDIT_RUN_PRINCIPAL** — {"type": "STRING scalar expression", "description": "Configured task principal Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **TASK_AUDIT_RUN_LEVEL** — {"type": "STRING scalar expression", "description": "Normalized run level, including HighestAvailable where explicitly configured Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **TASK_AUDIT_TASK_XML** — {"type": "STRING scalar expression", "description": "Complete original task XML for investigation Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **TASK_AUDIT_CONFIG_KEY** — {"type": "STRING scalar expression", "description": "Canonical configuration identity used by the approved baseline Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **APPROVED_TASKS_DATASET** — {"type": "XQL dataset identifier", "description": "Actual read-only source for APPROVED_TASKS. Bind a documented/inspected dataset or reviewed view; this name is not a claim of an existing integration.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **APPROVED_TASKS_APPROVED_HOST** — {"type": "STRING scalar expression", "description": "Same host scope as the task event Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **APPROVED_TASKS_APPROVED_CONFIG** — {"type": "STRING scalar expression", "description": "Exact configuration identity with author, actions, triggers and principal; one current approved row per key. This is a review-time snapshot, not historical approval at event time Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **APPROVED_TASKS_APPROVAL_ID** — {"type": "STRING scalar expression", "description": "Actual current approved task/deployment reference; never infer that approval existed when the historical event occurred Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}

[Unfilled bindings file](../bindings/n031.bindings.example.json). All binding values intentionally start as null.

## Pipeline and result semantics

Read the stages in order: the source and early filters establish the population; alter creates explicitly typed aliases; joins/union introduce related evidence; comp/windowcomp/dedup change row cardinality where present; the final projection, sort and limit define displayed rows. A result limit does not bound scanning or join work. Check the identity and time predicates in each subquery against the binding contract.

Estimated cost: MEDIUM. Bounded lookback and early filters; result caps do not bound upstream scanning. No measured performance claim.

## Limitations and tuning

- Bind Security-audit provider/channel and task event versions; the TASK_AUDIT source must exclude unrelated providers reusing IDs.
- The approved dataset must be a complete current snapshot for the evaluation scope; null config identity is UNASSESSED. Historical approval needs a separate effective-interval comparison.
- No tenant compilation or execution. Validate nulls, access, retention and source collection before interpreting empty results.
- A terminal limit bounds displayed output, not upstream scans or join multiplicity.

## Acceptance scenarios — NOT RUN

- {"kind": "positive", "fixture": "4698 creates a highest-run-level task with an unapproved executable/action configuration", "expected": "Review row with original XML and distinct subject/author."}
- {"kind": "negative", "fixture": "A process merely invokes schtasks but no configuration audit exists", "expected": "No task-creation row."}
- {"kind": "null", "fixture": "Task XML lacks a parsed configuration identity", "expected": "UNASSESSED; raw XML remains."}
- {"kind": "edge", "fixture": "4702 changes a second action or trigger while preserving task name", "expected": "Canonical config changes and the deviation remains visible."}

These scenarios are plans for representative source fixtures and bounded tenant runs. They have not been executed in Cortex. First inspect the bound schema, then compile the complete body, then exercise positive, benign, negative, null and boundary cases. Do not promote this interactive hunt directly to BIOC/correlation without reviewing that surface.

Independent static review: **PASS**. Full-query C/E: **NOT RUN / NOT RUN**. Static review cannot establish source population or detection effectiveness.

## ATT&CK context

{"id": "T1053.005", "technique": "Scheduled Task", "tactics": "Persistence (TA0003); Execution (TA0002)"}

Source workbook ATT&CK Enterprise v19.2 hypothesis retained for traceability. The query observes only the behavior in its coverage boundary; mapping is not certified detection coverage.

## References

- [Official source for TH-006](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields)
- [Official source for TH-006](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/action-actor)
- [Official source for TH-006](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/actor-actor)
- [Official source for TH-006](https://cortex-docs.paloaltonetworks.com/xql-command-reference-guide/readme/functions/timestamp_diff)
- [Official source for TH-006](https://docs-cortex.paloaltonetworks.com/r/Cortex/Cortex-XQL-Command-Reference/Example-2-Left-join?contentId=5iT66RH4ney5McWya7JKjQ)
- [Official source for TH-006](https://learn.microsoft.com/en-us/previous-versions/windows/it-pro/windows-10/security/threat-protection/auditing/event-4698)
- [Official source for TH-006](https://learn.microsoft.com/en-us/previous-versions/windows/it-pro/windows-10/security/threat-protection/auditing/event-4702)
- [Official source for TH-006](https://attack.mitre.org/techniques/T1053/005/)
- [XQL filter Example 22: ordinary double-quoted strings](https://docs-cortex.paloaltonetworks.com/r/Cortex/Cortex-XQL-Command-Reference/Example-22-String-manipulation-single-quotes?contentId=lwRG5fwKl6gNfg7ngRAAMA)
- [RE2 syntax reference](https://github.com/google/re2/wiki/syntax)
