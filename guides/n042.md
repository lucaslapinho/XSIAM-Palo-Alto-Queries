# N042 — Threat Hunting - Service-ticket Bursts and Rare Requesters

Compares successful service-ticket activity in fixed ten-minute windows with a covered prior requester/peer baseline, retaining all encryption types as context.

Proposal: **TH-017**. Category: **Threat Hunting / Credential Access**.

Artifact: **SCHEMA_BINDING_TEMPLATE**. Product/surface: Cortex XSIAM / Interactive XQL Search. Tenant release, full-query compiler acceptance and execution: **UNVERIFIED / NOT RUN**.

[Query file](../queries/threat-hunting/credential-access/n042-threat-hunting-service-ticket-bursts-and-rare-requesters.xql)

## Objective and interpretation

Complete burst/rare-requester baseline template. Service targets are the actual available target identifier, not invented SPNs; ticket requests do not prove cracking.

Output grain: See the query projection and aggregation.
Timeframe: 1d

## Source and field contract

Sources: `["{{SERVICE_TICKETS_DATASET}}", "{{TICKET_REQUESTER_BASELINE_DATASET}}"]`.

Every schema token must resolve to an inspected field, expression or source with the stated type and meaning. A normalized source contract may require telemetry ingestion, parsing or a separately reviewed view; replacing names alone cannot create missing evidence. Preserve nulls. Verify timezone, identity namespaces, provider/channel, event outcomes, source completeness and row multiplicity.

- **SERVICE_TICKETS_DATASET** — {"type": "XQL dataset identifier", "description": "Actual read-only source for SERVICE_TICKETS. Bind a documented/inspected dataset or reviewed view; this name is not a claim of an existing integration.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **SERVICE_TICKETS_TICKET_TIME** — {"type": "DATETIME scalar expression", "description": "Original 4769 event time Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **SERVICE_TICKETS_REQUESTER** — {"type": "STRING scalar expression", "description": "Normalized domain-qualified requester identity; event name is not assumed an authoritative UPN Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **SERVICE_TICKETS_SOURCE_IP** — {"type": "STRING scalar expression", "description": "Observed IPv4/IPv6 client address Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **SERVICE_TICKETS_SERVICE_TARGET** — {"type": "STRING scalar expression", "description": "Service account/SID or verified SPN target; do not label an account name as a full SPN Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **SERVICE_TICKETS_ENCRYPTION_TYPE** — {"type": "STRING scalar expression", "description": "Original ticket encryption representation including AES/RC4 Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **SERVICE_TICKETS_EVENT_ID** — {"type": "INTEGER scalar expression", "description": "Provider-verified event ID Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **SERVICE_TICKETS_SUCCESS** — {"type": "BOOLEAN scalar expression", "description": "Success mapped from the actual event-version Status value Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **TICKET_REQUESTER_BASELINE_DATASET** — {"type": "XQL dataset identifier", "description": "Actual read-only source for TICKET_REQUESTER_BASELINE. Bind a documented/inspected dataset or reviewed view; this name is not a claim of an existing integration.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **TICKET_REQUESTER_BASELINE_B_REQUESTER** — {"type": "STRING scalar expression", "description": "Same normalized requester key; exactly one baseline row per key, with an explicit zero row required to establish rarity Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **TICKET_REQUESTER_BASELINE_BASELINE_DAYS** — {"type": "INTEGER scalar expression", "description": "Covered prior days strictly before the rolling current window [evaluation_time minus 86400 seconds, evaluation_time); freeze one comparable baseline snapshot ending at or before that boundary, not an overlapping calendar day Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **TICKET_REQUESTER_BASELINE_PRIOR_REQUEST_ROWS** — {"type": "INTEGER scalar expression", "description": "Requester successful request volume over the prior baseline Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **TICKET_REQUESTER_BASELINE_EXPECTED_PEAK_TARGETS** — {"type": "INTEGER scalar expression", "description": "Reviewed expected peak distinct service targets in comparable ten-minute windows entirely before the rolling current interval; nonnegative and sourced from the same frozen baseline snapshot Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **TICKET_REQUESTER_BASELINE_BASELINE_COMPLETE** — {"type": "BOOLEAN scalar expression", "description": "Collection and peer comparability validated for the intended baseline Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **MIN_DISTINCT_SERVICES** — {"type": "INTEGER literal", "description": "Positive triage floor, for example 10 distinct service targets per fixed ten-minute window; tune against actual benign workloads", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **BURST_MULTIPLIER** — {"type": "NUMBER literal", "description": "Positive multiplier over reviewed expected peak, for example 3; not a detection-quality guarantee", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}

[Unfilled bindings file](../bindings/n042.bindings.example.json). All binding values intentionally start as null.

## Pipeline and result semantics

Read the stages in order: the source and early filters establish the population; alter creates explicitly typed aliases; joins/union introduce related evidence; comp/windowcomp/dedup change row cardinality where present; the final projection, sort and limit define displayed rows. A result limit does not bound scanning or join work. Check the identity and time predicates in each subquery against the binding contract.

Estimated cost: MEDIUM. Bounded lookback and early filters; result caps do not bound upstream scanning. No measured performance claim.

## Limitations and tuning

- Use disjoint baseline/current intervals and version-aware 4769 field maps. AES is retained; RC4 is not a required condition.
- Fixed bins split boundary-spanning bursts. Null/incomplete baselines yield BASELINE_UNASSESSED, not automatic rarity.
- No tenant compilation or execution. Validate nulls, access, retention and source collection before interpreting empty results.
- A terminal limit bounds displayed output, not upstream scans or join multiplicity.

## Acceptance scenarios — NOT RUN

- {"kind": "positive", "fixture": "Requester hits 30 distinct targets when expected peak is 4, threshold 10 and multiplier 3", "expected": "Burst candidate regardless of AES or RC4."}
- {"kind": "negative", "fixture": "Known high-volume service stays within its reviewed peak", "expected": "Not flagged as a burst."}
- {"kind": "null", "fixture": "No complete prior baseline", "expected": "Explicit BASELINE_UNASSESSED rather than rare."}
- {"kind": "edge", "fixture": "Requests straddle two ten-minute bins", "expected": "Counts remain per fixed bin; boundary limitation is documented."}

These scenarios are plans for representative source fixtures and bounded tenant runs. They have not been executed in Cortex. First inspect the bound schema, then compile the complete body, then exercise positive, benign, negative, null and boundary cases. Do not promote this interactive hunt directly to BIOC/correlation without reviewing that surface.

Independent static review: **PASS**. Full-query C/E: **NOT RUN / NOT RUN**. Static review cannot establish source population or detection effectiveness.

## ATT&CK context

{"id": "T1558.003", "technique": "Kerberoasting", "tactics": "Credential Access (TA0006)"}

Source workbook ATT&CK Enterprise v19.2 hypothesis retained for traceability. The query observes only the behavior in its coverage boundary; mapping is not certified detection coverage.

## References

- [Official source for TH-017](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields)
- [Official source for TH-017](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/action-actor)
- [Official source for TH-017](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/actor-actor)
- [Official source for TH-017](https://cortex-docs.paloaltonetworks.com/xql-command-reference-guide/readme/functions/timestamp_diff)
- [Official source for TH-017](https://docs-cortex.paloaltonetworks.com/r/Cortex/Cortex-XQL-Command-Reference/Example-2-Left-join?contentId=5iT66RH4ney5McWya7JKjQ)
- [Official source for TH-017](https://learn.microsoft.com/en-us/previous-versions/windows/it-pro/windows-10/security/threat-protection/auditing/event-4769)
- [Official source for TH-017](https://attack.mitre.org/techniques/T1558/003/)
