# N053 — SMB File Write Followed by Service Execution - Schema Binding Template

Correlates a completed SMB write, successful installation of a service using the same resolved executable path, and a verified service-associated process start in bounded sequence.

Proposal: **TH-029**. Category: **Threat Hunting / Lateral Movement**.

Artifact: **SCHEMA_BINDING_TEMPLATE**. Product/surface: Cortex XSIAM / Interactive XQL Search. Tenant release, full-query compiler acceptance and execution: **UNVERIFIED / NOT RUN**.

[Query file](../queries/threat-hunting/lateral-movement/n053-smb-file-write-followed-by-service-execution-schema-binding-template.xql)

## Objective and interpretation

One write/install/start evidence chain. A maximum ten-minute gap is enforced at each transition; many valid pairs can remain.

Output grain: One write/install/start evidence chain. A maximum ten-minute gap is enforced at each transition; many valid pairs can remain.
Timeframe: 1d unless explicitly expanded in body

## Source and field contract

Sources: `["{{SMB_FILE_WRITE_DATASET}}", "{{SERVICE_INSTALL_DATASET}}", "{{SERVICE_PROCESS_START_DATASET}}"]`.

Every schema token must resolve to an inspected field, expression or source with the stated type and meaning. A normalized source contract may require telemetry ingestion, parsing or a separately reviewed view; replacing names alone cannot create missing evidence. Preserve nulls. Verify timezone, identity namespaces, provider/channel, event outcomes, source completeness and row multiplicity.

- **SMB_FILE_WRITE_DATASET** — {"type": "DATASET_IDENTIFIER", "kind": "DATASET", "binding_contract": "Completed SMB file-write/transfer telemetry with server-side canonical path and authenticated context. A 5145 access check alone is insufficient.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SMB_WRITE_TIME** — {"type": "DATETIME", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Completed write source time.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SMB_WRITE_UID** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Unique write/transfer evidence ID. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SMB_TARGET_ENDPOINT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Stable destination server. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SMB_WRITE_SOURCE** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Canonical authenticated client IP. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SMB_WRITE_ACCOUNT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Authenticated writer identity. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SMB_SERVER_FILE_PATH** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Canonical destination executable path, matched to service executable after resolving arguments/environment variables. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder. Resolve Windows case-insensitive identity into the same canonical key on all three sources before this exact comparison; preserve case where a case-sensitive directory is enabled.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SMB_WRITE_RESULT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "success only for confirmed completed content write; access_granted differs. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SERVICE_INSTALL_DATASET** — {"type": "DATASET_IDENTIFIER", "kind": "DATASET", "binding_contract": "Collected service-install events such as Windows Security 4697 with service executable separated from arguments.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SERVICE_INSTALL_TIME** — {"type": "DATETIME", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Service installation source time.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SERVICE_INSTALL_UID** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Unique service-install event. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SERVICE_ENDPOINT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Destination stable server. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SERVICE_NAME** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Installed service identifier. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder. Resolve Windows case-insensitive identity into the same canonical key on all three sources before this exact comparison; preserve case where a case-sensitive directory is enabled.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SERVICE_EXECUTABLE_PATH** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Resolved canonical executable path, not full image command line. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder. Resolve Windows case-insensitive identity into the same canonical key on all three sources before this exact comparison; preserve case where a case-sensitive directory is enabled.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SERVICE_INSTALL_RESULT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "success only for verified installed service event. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SERVICE_ACCOUNT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Configured service principal. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SERVICE_PROCESS_START_DATASET** — {"type": "DATASET_IDENTIFIER", "kind": "DATASET", "binding_contract": "Process-start telemetry with verified service-name association, not arbitrary same-path execution.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SERVICE_START_TIME** — {"type": "DATETIME", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Process-start source time.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SERVICE_START_ENDPOINT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Destination server identity. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **STARTED_SERVICE_NAME** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Verified service association matching installation service ID. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder. Resolve Windows case-insensitive identity into the same canonical key on all three sources before this exact comparison; preserve case where a case-sensitive directory is enabled.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **STARTED_SERVICE_PATH** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Resolved executable canonical path. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder. Resolve Windows case-insensitive identity into the same canonical key on all three sources before this exact comparison; preserve case where a case-sensitive directory is enabled.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SERVICE_START_INSTANCE** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Stable process-start instance ID. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}

[Unfilled bindings file](../bindings/n053.bindings.example.json). All binding values intentionally start as null.

## Pipeline and result semantics

Read the stages in order: the source and early filters establish the population; alter creates explicitly typed aliases; joins/union introduce related evidence; comp/windowcomp/dedup change row cardinality where present; the final projection, sort and limit define displayed rows. A result limit does not bound scanning or join work. Check the identity and time predicates in each subquery against the binding contract.

Estimated cost: HIGH. Bounded lookback and early filters; result caps do not bound upstream scanning. No measured performance claim.

## Limitations and tuning

- Deployment tools commonly create this sequence; no PsExec filename dependency or maliciousness claim.
- Path equivalence does not prove bytes are unchanged between events; add hash verification before claiming exact transferred-file identity.
- 5145 access authorization and a bare 4697 installation cannot substitute for completed write or actual service-associated execution.
- All bindings are explicit contracts, not claims that a source or field exists. Bind field/expression placeholders to typed tenant fields or tested extraction expressions; never substitute a made-up name.
- One-day telemetry lookback unless stated otherwise. Source-clock, retention, collection and permission gaps can remove evidence; _time filtering must cover each source event time.
- All canonical normalized words in expressions are local mapping contracts, not undocumented Cortex enum values.
- Result limits apply after analysis and do not cap scanned data or join fan-out. No tenant compile, execution, performance or deployment claim.
- ATT&CK, where present, is an analytical interpretation requiring context, not proof of compromise.
- Inventory/coverage/snapshot source bindings must return every relevant validity record under the configured query timeframe; if their storage timestamps differ, establish and test an explicit inner timeframe before use. A missing lookup row is never automatically benign.
- Comparisons are case-sensitive. Binding owners must canonicalize values only in source namespaces proven case-insensitive (such as reviewed Windows service/path/account keys and DNS A-label names), identically on both join sides. Preserve Unix paths, case-sensitive identities, ARN resource components, and source evidence text. All normalized operation/result labels must match the documented local contract spelling exactly.
- Time-difference bounds are expressed at second resolution. Validate subsecond boundary behavior using source fixtures; direct timestamp comparisons enforce before/after ordering where required.

## Acceptance scenarios — NOT RUN

- {"kind": "positive", "fixture": "A completed SMB write at 10:00 is installed as service S at 10:02 and S starts at 10:03 on the same host/path.", "expected": "One complete sequence.", "status": "NOT RUN"}
- {"kind": "negative", "fixture": "Only access-granted 5145 plus a service install appears, or no service process starts.", "expected": "Excluded.", "status": "NOT RUN"}
- {"kind": "null", "fixture": "Service executable cannot be separated from command-line arguments or its start cannot be associated with S.", "expected": "Required binding unresolved; no valid chain.", "status": "NOT RUN"}
- {"kind": "edge", "fixture": "Two writes update the same path within ten minutes before installation.", "expected": "Both candidate chains can appear; do not report unique transferred payload identity.", "status": "NOT RUN"}

These scenarios are plans for representative source fixtures and bounded tenant runs. They have not been executed in Cortex. First inspect the bound schema, then compile the complete body, then exercise positive, benign, negative, null and boundary cases. Do not promote this interactive hunt directly to BIOC/correlation without reviewing that surface.

Independent static review: **PASS**. Full-query C/E: **NOT RUN / NOT RUN**. Static review cannot establish source population or detection effectiveness.

## ATT&CK context

{"id": "T1021.002; T1569.002", "technique": "SMB/Windows Admin Shares; Service Execution", "tactics": "Lateral Movement (TA0008); Execution (TA0002)"}

Source workbook ATT&CK Enterprise v19.2 hypothesis retained for traceability. The query observes only the behavior in its coverage boundary; mapping is not certified detection coverage.

## References

- [Microsoft event 5145](https://learn.microsoft.com/en-us/previous-versions/windows/it-pro/windows-10/security/threat-protection/auditing/event-5145)
- [Microsoft event 4697](https://learn.microsoft.com/en-us/previous-versions/windows/it-pro/windows-10/security/threat-protection/auditing/event-4697)
- [Cortex action schema](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/action-actor)
- [Cortex XSIAM comp](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/comp?contentId=6TGIbW601nVvh2ycedN_hw)
- [Cortex XSIAM join](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/join?contentId=MyWFbdks6xgkqm_1lNkfGg)
- [Cortex XSIAM bin](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/bin?contentId=CU_u_bTkg_MICr5pUKivwg)
- [Cortex XSIAM sort](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/sort?contentId=a9tB71nqIWhA~Adcae6R7w)
