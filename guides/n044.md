# N044 — Password Spraying Across Accounts - Schema Binding Template

Ranks source-specific bad-secret failures spread across accounts within 15-minute bins. Retains original reasons and separates source systems; thresholds identify spray-shaped behavior for review.

Proposal: **TH-019**. Category: **Threat Hunting / Credential Access**.

Artifact: **SCHEMA_BINDING_TEMPLATE**. Product/surface: Cortex XSIAM / Interactive XQL Search. Tenant release, full-query compiler acceptance and execution: **UNVERIFIED / NOT RUN**.

[Query file](../queries/threat-hunting/credential-access/n044-password-spraying-across-accounts-schema-binding-template.xql)

## Objective and interpretation

One source-system/client/time-bin with many target accounts and low average retries. Slow and distributed spraying needs a separate longer-window baseline.

Output grain: One source-system/client/time-bin with many target accounts and low average retries. Slow and distributed spraying needs a separate longer-window baseline.
Timeframe: 1d unless explicitly expanded in body

## Source and field contract

Sources: `["{{AUTH_FAILURE_DATASET}}"]`.

Every schema token must resolve to an inspected field, expression or source with the stated type and meaning. A normalized source contract may require telemetry ingestion, parsing or a separately reviewed view; replacing names alone cannot create missing evidence. Preserve nulls. Verify timezone, identity namespaces, provider/channel, event outcomes, source completeness and row multiplicity.

- **AUTH_FAILURE_DATASET** — {"type": "DATASET_IDENTIFIER", "kind": "DATASET", "binding_contract": "One source-specific authentication audit dataset or a verified union preserving source_system.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **FAIL_TIME** — {"type": "DATETIME", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Source authentication timestamp.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **FAIL_EVENT_UID** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Source-system-scoped unique event key; no cross-provider dedup by account/time. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **FAIL_SOURCE_SYSTEM** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Original provider/product and authentication mechanism. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **FAIL_SOURCE_IP** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Canonical client IP; unknown/dash to null; preserve source-specific NAT context. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **FAIL_ACCOUNT_KEY** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Realm-qualified target account, not reporting actor. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **FAIL_OUTCOME** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Normalize verified failure events to failure; successes to success; unknown stays null. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **FAIL_REASON_CLASS** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Normalize wrong-password/bad-secret failures to bad_secret using source-specific verified status/substatus; lockout/unknown-user/unrelated errors stay separate. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **FAIL_REASON_RAW** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Original status and substatus or reason code for audit. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **MIN_ACCOUNTS** — {"type": "INTEGER_LITERAL", "kind": "VALUE_OR_LITERAL", "binding_contract": "Set positive peer-reviewed minimum distinct target accounts; test example 10.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **MAX_FAILURES_PER_ACCOUNT** — {"type": "NUMBER_LITERAL", "kind": "VALUE_OR_LITERAL", "binding_contract": "Positive upper average retries consistent with spray hypothesis, example 3; not a universal security threshold.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}

[Unfilled bindings file](../bindings/n044.bindings.example.json). All binding values intentionally start as null.

## Pipeline and result semantics

Read the stages in order: the source and early filters establish the population; alter creates explicitly typed aliases; joins/union introduce related evidence; comp/windowcomp/dedup change row cardinality where present; the final projection, sort and limit define displayed rows. A result limit does not bound scanning or join work. Check the identity and time predicates in each subquery against the binding contract.

Estimated cost: MEDIUM. Bounded lookback and early filters; result caps do not bound upstream scanning. No measured performance claim.

## Limitations and tuning

- NAT, shared gateways and stale service secrets can create legitimate fan-out. A count cannot establish that one password was reused.
- Account identity and failure-code normalization must be validated separately for 4625, 4771 and other providers.
- All bindings are explicit contracts, not claims that a source or field exists. Bind field/expression placeholders to typed tenant fields or tested extraction expressions; never substitute a made-up name.
- One-day telemetry lookback unless stated otherwise. Source-clock, retention, collection and permission gaps can remove evidence; _time filtering must cover each source event time.
- All canonical normalized words in expressions are local mapping contracts, not undocumented Cortex enum values.
- Result limits apply after analysis and do not cap scanned data or join fan-out. No tenant compile, execution, performance or deployment claim.
- ATT&CK, where present, is an analytical interpretation requiring context, not proof of compromise.
- Inventory/coverage/snapshot source bindings must return every relevant validity record under the configured query timeframe; if their storage timestamps differ, establish and test an explicit inner timeframe before use. A missing lookup row is never automatically benign.
- Comparisons are case-sensitive. Binding owners must canonicalize values only in source namespaces proven case-insensitive (such as reviewed Windows service/path/account keys and DNS A-label names), identically on both join sides. Preserve Unix paths, case-sensitive identities, ARN resource components, and source evidence text. All normalized operation/result labels must match the documented local contract spelling exactly.
- Time-difference bounds are expressed at second resolution. Validate subsecond boundary behavior using source fixtures; direct timestamp comparisons enforce before/after ordering where required.

## Acceptance scenarios — NOT RUN

- {"kind": "positive", "fixture": "Ten accounts receive one bad-secret failure from source A in one bin; thresholds 10 and 3.", "expected": "One group with account_count 10 and failure_events 10.", "status": "NOT RUN"}
- {"kind": "negative", "fixture": "One account receives 30 failures, or ten lockout-only failures occur.", "expected": "No qualifying group.", "status": "NOT RUN"}
- {"kind": "null", "fixture": "Source IP or target account is missing.", "expected": "Excluded from grouping; record the resulting coverage gap.", "status": "NOT RUN"}
- {"kind": "edge", "fixture": "The same event is delivered twice and another provider reports its own failure.", "expected": "Repeated delivery is deduplicated per source; different providers remain separate.", "status": "NOT RUN"}

These scenarios are plans for representative source fixtures and bounded tenant runs. They have not been executed in Cortex. First inspect the bound schema, then compile the complete body, then exercise positive, benign, negative, null and boundary cases. Do not promote this interactive hunt directly to BIOC/correlation without reviewing that surface.

Independent static review: **PASS**. Full-query C/E: **NOT RUN / NOT RUN**. Static review cannot establish source population or detection effectiveness.

## ATT&CK context

{"id": "T1110.003", "technique": "Password Spraying", "tactics": "Credential Access (TA0006)"}

Source workbook ATT&CK Enterprise v19.2 hypothesis retained for traceability. The query observes only the behavior in its coverage boundary; mapping is not certified detection coverage.

## References

- [Microsoft event 4625](https://learn.microsoft.com/en-us/previous-versions/windows/it-pro/windows-10/security/threat-protection/auditing/event-4625)
- [Cortex XSIAM comp](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/comp?contentId=6TGIbW601nVvh2ycedN_hw)
- [Cortex XSIAM join](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/join?contentId=MyWFbdks6xgkqm_1lNkfGg)
- [Cortex XSIAM bin](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/bin?contentId=CU_u_bTkg_MICr5pUKivwg)
- [Cortex XSIAM sort](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/sort?contentId=a9tB71nqIWhA~Adcae6R7w)
- [MITRE Password Spraying](https://attack.mitre.org/techniques/T1110/003/)
