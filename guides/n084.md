# N084 — Investigation - File Creation Write Rename and Deletion Timeline

Returns validated create/write/rename/delete operations for an exact path on a selected endpoint, preserving both rename paths, producing process, actor and outcome.

Proposal: **OP-014**. Category: **Investigation / File Activity**.

Artifact: **SCHEMA_BINDING_TEMPLATE**. Product/surface: Cortex XSIAM / Interactive XQL Search. Tenant release, full-query compiler acceptance and execution: **UNVERIFIED / NOT RUN**.

[Query file](../queries/investigation/file-activity/n084-investigation-file-creation-write-rename-and-deletion-timeline.xql)

## Objective and interpretation

Full path-based operation timeline template. Actor/process output provides the process pivot; operation coverage must be bound independently.

Output grain: See the query projection and aggregation.
Timeframe: 1d

## Source and field contract

Sources: `["{{FILE_OPERATIONS_DATASET}}"]`.

Every schema token must resolve to an inspected field, expression or source with the stated type and meaning. A normalized source contract may require telemetry ingestion, parsing or a separately reviewed view; replacing names alone cannot create missing evidence. Preserve nulls. Verify timezone, identity namespaces, provider/channel, event outcomes, source completeness and row multiplicity.

- **FILE_OPERATIONS_DATASET** — {"type": "XQL dataset identifier", "description": "Actual read-only source for FILE_OPERATIONS. Bind a documented/inspected dataset or reviewed view; this name is not a claim of an existing integration.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **FILE_OPERATIONS_EVENT_TIME** — {"type": "DATETIME scalar expression", "description": "Observed file operation time Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **FILE_OPERATIONS_HOST_ID** — {"type": "STRING scalar expression", "description": "Stable endpoint ID Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **FILE_OPERATIONS_OPERATION** — {"type": "STRING scalar expression", "description": "CREATE, WRITE, RENAME, DELETE derived from verified event category/subtype Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **FILE_OPERATIONS_OLD_PATH** — {"type": "STRING scalar expression", "description": "Canonical previous path for rename/delete when observed. Preserve case for case-sensitive namespaces; fold case only after verifying a case-insensitive Windows namespace and apply identical normalization to the selected path Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **FILE_OPERATIONS_NEW_PATH** — {"type": "STRING scalar expression", "description": "Canonical current/destination path for create/write/rename. Preserve case for case-sensitive namespaces; fold case only after verifying a case-insensitive Windows namespace and apply identical normalization to the selected path Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **FILE_OPERATIONS_FILE_HASH** — {"type": "STRING scalar expression", "description": "SHA-256 if observed; never infer after delete Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **FILE_OPERATIONS_PROCESS_ID** — {"type": "STRING scalar expression", "description": "Actual producing process instance Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **FILE_OPERATIONS_WRITER** — {"type": "STRING scalar expression", "description": "Actual effective actor identity Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **FILE_OPERATIONS_OUTCOME** — {"type": "STRING scalar expression", "description": "Original success/failure/result meaning Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **FILE_REVIEW_ENDPOINT** — {"type": "STRING literal", "description": "Quoted stable endpoint ID", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **FILE_REVIEW_EXACT_PATH** — {"type": "STRING literal", "description": "Quoted exact path in the same bound canonicalization as old_path/new_path. Preserve case for case-sensitive namespaces; fold case only for verified case-insensitive Windows paths on all three sides", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}

[Unfilled bindings file](../bindings/n084.bindings.example.json). All binding values intentionally start as null.

## Pipeline and result semantics

Read the stages in order: the source and early filters establish the population; alter creates explicitly typed aliases; joins/union introduce related evidence; comp/windowcomp/dedup change row cardinality where present; the final projection, sort and limit define displayed rows. A result limit does not bound scanning or join work. Check the identity and time predicates in each subquery against the binding contract.

Estimated cost: MEDIUM. Bounded lookback and early filters; result caps do not bound upstream scanning. No measured performance claim.

## Limitations and tuning

- Global case-sensitive comparison preserves Linux path distinctions. Case-insensitive Windows namespaces require explicit identical normalization in old_path, new_path and the selected path; never fold case for a case-sensitive directory.
- File reads are not included; deletion may legitimately lack hash/current path. Unknown outcomes remain original values.
- No tenant compilation or execution. Validate nulls, access, retention and source collection before interpreting empty results.
- A terminal limit bounds displayed output, not upstream scans or join multiplicity.

## Acceptance scenarios — NOT RUN

- {"kind": "positive", "fixture": "File is created, written, renamed away from the selected path and later deleted at that path", "expected": "Each matching old/new-path operation is returned."}
- {"kind": "negative", "fixture": "Read event or similar substring path", "expected": "Excluded."}
- {"kind": "null", "fixture": "Deletion has old_path but no new_path/hash", "expected": "Deletion remains."}
- {"kind": "edge", "fixture": "Rename enters versus leaves the selected path; a Linux path differs only in case", "expected": "Rename matches through new_path or old_path respectively; different-case Linux path does not match."}

These scenarios are plans for representative source fixtures and bounded tenant runs. They have not been executed in Cortex. First inspect the bound schema, then compile the complete body, then exercise positive, benign, negative, null and boundary cases. Do not promote this interactive hunt directly to BIOC/correlation without reviewing that surface.

Independent static review: **PASS**. Full-query C/E: **NOT RUN / NOT RUN**. Static review cannot establish source population or detection effectiveness.

## ATT&CK context

N/A — operational investigation

Operational visibility does not imply adversary behavior.

## References

- [Official source for OP-014](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields)
- [Official source for OP-014](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/action-actor)
- [Official source for OP-014](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/actor-actor)
- [Official source for OP-014](https://cortex-docs.paloaltonetworks.com/xql-command-reference-guide/readme/functions/timestamp_diff)
