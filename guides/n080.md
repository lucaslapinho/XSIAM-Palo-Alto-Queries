# N080 — Firewall Blocks and Resets by Initiating Host - Schema Binding Template

Aggregates verified firewall deny/drop/reset log records by time-valid initiating-host attribution, rule and action, keeping resets separate from policy blocks and unresolved IP attribution explicit.

Proposal: **OP-009**. Category: **Investigation / Firewall**.

Artifact: **SCHEMA_BINDING_TEMPLATE**. Product/surface: Cortex XSIAM / Interactive XQL Search. Tenant release, full-query compiler acceptance and execution: **UNVERIFIED / NOT RUN**.

[Query file](../queries/investigation/firewall/n080-firewall-blocks-and-resets-by-initiating-host-schema-binding-template.xql)

## Objective and interpretation

One initiating-host/source-IP/firewall/rule/application/protocol/action group; metric is unique source log-record count after duplicate-delivery removal.

Output grain: One initiating-host/source-IP/firewall/rule/application/protocol/action group; metric is unique source log-record count after duplicate-delivery removal.
Timeframe: 1d unless explicitly expanded in body

## Source and field contract

Sources: `["{{FIREWALL_TRAFFIC_DATASET}}", "{{SOURCE_IP_OWNERSHIP}}"]`.

Every schema token must resolve to an inspected field, expression or source with the stated type and meaning. A normalized source contract may require telemetry ingestion, parsing or a separately reviewed view; replacing names alone cannot create missing evidence. Preserve nulls. Verify timezone, identity namespaces, provider/channel, event outcomes, source completeness and row multiplicity.

- **FIREWALL_TRAFFIC_DATASET** — {"type": "DATASET_IDENTIFIER", "kind": "DATASET", "binding_contract": "Verified firewall traffic events, retaining source vendor/device, exact record identity and raw action; endpoint network flags are not firewall decisions.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **FW_TIME** — {"type": "DATETIME", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Firewall-generated event time.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **FW_EVENT_UID** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Device/vsys/log-type/generation scoped unique log ID; session ID alone is insufficient. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **FW_DEVICE** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Stable firewall/device scope. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **FW_CLIENT_IP** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Original client IP at the mapped identity boundary, with NAT policy documented. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **FW_SERVER_IP** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Original destination/server IP. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **FW_SERVER_PORT** — {"type": "INTEGER", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Destination port with validated numeric conversion.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **FW_PROTOCOL** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Normalized transport identifier while retaining raw-source evidence externally. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **FW_ACTION_RAW** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Original action value. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **FW_ACTION_CLASS** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Verified mapping to deny, drop, reset_client, reset_server, reset_both; allow and unknown stay distinct. Do not map every reset to deny. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **FW_RULE_ID** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Stable rule ID or explicit source-scoped name if ID unavailable. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **FW_APPLICATION** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Actual firewall application classification, nullable. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SOURCE_IP_OWNERSHIP** — {"type": "DATASET_IDENTIFIER", "kind": "DATASET", "binding_contract": "Authoritative time-valid client IP to endpoint ownership at the firewall address boundary; nonoverlapping assignments required.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **OWNER_IP** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Canonical IP matching original source boundary, not arbitrary translated address. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **OWNER_ENDPOINT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Stable endpoint ID. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **OWNER_HOSTNAME** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Observed name for context. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **OWNER_FROM** — {"type": "DATETIME", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Inclusive ownership start.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **OWNER_TO** — {"type": "DATETIME", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Exclusive ownership end.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}

[Unfilled bindings file](../bindings/n080.bindings.example.json). All binding values intentionally start as null.

## Pipeline and result semantics

Read the stages in order: the source and early filters establish the population; alter creates explicitly typed aliases; joins/union introduce related evidence; comp/windowcomp/dedup change row cardinality where present; the final projection, sort and limit define displayed rows. A result limit does not bound scanning or join work. Check the identity and time predicates in each subquery against the binding contract.

Estimated cost: HIGH. Bounded lookback and early filters; result caps do not bound upstream scanning. No measured performance claim.

## Limitations and tuning

- Firewall reset meanings are vendor-specific; reset records remain RESET rather than automatic policy-deny evidence.
- Source IP ownership must match the correct NAT/address boundary. Ambiguous mappings must not produce asserted host identity.
- Log repeat counters/session start/end records have different cardinality from log rows; this query does not count unique blocked sessions.
- Unresolved IP groups are not verified hosts.
- All bindings are explicit contracts, not claims that a source or field exists. Bind field/expression placeholders to typed tenant fields or tested extraction expressions; never substitute a made-up name.
- One-day telemetry lookback unless stated otherwise. Source-clock, retention, collection and permission gaps can remove evidence; _time filtering must cover each source event time.
- All canonical normalized words in expressions are local mapping contracts, not undocumented Cortex enum values.
- Result limits apply after analysis and do not cap scanned data or join fan-out. No tenant compile, execution, performance or deployment claim.
- ATT&CK, where present, is an analytical interpretation requiring context, not proof of compromise.
- Inventory/coverage/snapshot source bindings must return every relevant validity record under the configured query timeframe; if their storage timestamps differ, establish and test an explicit inner timeframe before use. A missing lookup row is never automatically benign.
- Comparisons are case-sensitive. Binding owners must canonicalize values only in source namespaces proven case-insensitive (such as reviewed Windows service/path/account keys and DNS A-label names), identically on both join sides. Preserve Unix paths, case-sensitive identities, ARN resource components, and source evidence text. All normalized operation/result labels must match the documented local contract spelling exactly.
- Time-difference bounds are expressed at second resolution. Validate subsecond boundary behavior using source fixtures; direct timestamp comparisons enforce before/after ordering where required.

## Acceptance scenarios — NOT RUN

- {"kind": "positive", "fixture": "Three unique deny records map to H and two reset_client records map to the same H/rule.", "expected": "Separate BLOCK count 3 and RESET count 2 groups.", "status": "NOT RUN"}
- {"kind": "negative", "fixture": "Allowed traffic or endpoint network_success=false lacks a firewall denial record.", "expected": "Excluded.", "status": "NOT RUN"}
- {"kind": "null", "fixture": "Valid block record has no time-valid ownership mapping.", "expected": "Retained under UNRESOLVED_IP with no invented hostname.", "status": "NOT RUN"}
- {"kind": "edge", "fixture": "Same firewall log is delivered twice, and two hosts sequentially own its source IP.", "expected": "Duplicate delivery is removed; event-time ownership assigns only the valid host.", "status": "NOT RUN"}

These scenarios are plans for representative source fixtures and bounded tenant runs. They have not been executed in Cortex. First inspect the bound schema, then compile the complete body, then exercise positive, benign, negative, null and boundary cases. Do not promote this interactive hunt directly to BIOC/correlation without reviewing that surface.

Independent static review: **PASS**. Full-query C/E: **NOT RUN / NOT RUN**. Static review cannot establish source population or detection effectiveness.

## ATT&CK context

N/A — operational investigation

Operational visibility does not imply adversary behavior.

## References

- [PAN-OS traffic log fields](https://docs.paloaltonetworks.com/ngfw/administration/monitoring/use-syslog-for-monitoring/syslog-field-descriptions/traffic-log-fields)
- [Cortex XSIAM comp](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/comp?contentId=6TGIbW601nVvh2ycedN_hw)
- [Cortex XSIAM join](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/join?contentId=MyWFbdks6xgkqm_1lNkfGg)
- [Cortex XSIAM bin](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/bin?contentId=CU_u_bTkg_MICr5pUKivwg)
- [Cortex XSIAM sort](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/sort?contentId=a9tB71nqIWhA~Adcae6R7w)
