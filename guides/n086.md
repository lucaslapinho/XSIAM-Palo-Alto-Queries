# N086 — Asset Visibility - All Reported Software Versions and Asset Groups

Lists all reported applications, vendors and versions from each endpoint's authoritative latest nonempty application snapshot and enriches actual asset-group membership. Explicitly empty latest reports do not revive older applications.

Proposal: **OP-016**. Category: **Asset Visibility / Software Inventory**.

Artifact: **SCHEMA_BINDING_TEMPLATE**. Product/surface: Cortex XSIAM / Interactive XQL Search. Tenant release, full-query compiler acceptance and execution: **UNVERIFIED / NOT RUN**.

[Query file](../queries/asset-visibility/software-inventory/n086-asset-visibility-all-reported-software-versions-and-asset-groups.xql)

## Objective and interpretation

General all-software inventory, not the earlier mapped-tool subset. Exact latest snapshot state including empty reports and group membership remain required bindings.

Output grain: See the query projection and aggregation.
Timeframe: 30d

## Source and field contract

Sources: `["host_inventory_applications", "{{APPLICATION_SNAPSHOT_STATE_DATASET}}", "{{ENDPOINT_GROUP_MEMBERSHIP_DATASET}}"]`.

Every schema token must resolve to an inspected field, expression or source with the stated type and meaning. A normalized source contract may require telemetry ingestion, parsing or a separately reviewed view; replacing names alone cannot create missing evidence. Preserve nulls. Verify timezone, identity namespaces, provider/channel, event outcomes, source completeness and row multiplicity.

- **APPLICATION_SNAPSHOT_STATE_DATASET** — {"type": "XQL dataset identifier", "description": "Actual read-only source for APPLICATION_SNAPSHOT_STATE. Bind a documented/inspected dataset or reviewed view; this name is not a claim of an existing integration.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **APPLICATION_SNAPSHOT_STATE_SNAPSHOT_ENDPOINT** — {"type": "STRING scalar expression", "description": "Endpoint identity matching host_inventory_applications.endpoint_id Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **APPLICATION_SNAPSHOT_STATE_SNAPSHOT_TIME** — {"type": "DATETIME scalar expression", "description": "Actual latest application snapshot timestamp including empty reports; one current row per endpoint Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **APPLICATION_SNAPSHOT_STATE_SNAPSHOT_EMPTY** — {"type": "BOOLEAN scalar expression", "description": "True only when the actual latest report explicitly contains zero applications Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **ENDPOINT_GROUP_MEMBERSHIP_DATASET** — {"type": "XQL dataset identifier", "description": "Actual read-only source for ENDPOINT_GROUP_MEMBERSHIP. Bind a documented/inspected dataset or reviewed view; this name is not a claim of an existing integration.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **ENDPOINT_GROUP_MEMBERSHIP_GROUP_ENDPOINT** — {"type": "STRING scalar expression", "description": "Same endpoint identity Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **ENDPOINT_GROUP_MEMBERSHIP_GROUP_NAME** — {"type": "STRING scalar expression", "description": "Actual human-readable asset group name; one row per membership Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **ENDPOINT_GROUP_MEMBERSHIP_GROUP_ID** — {"type": "STRING scalar expression", "description": "Actual asset group ID normalized without identity collisions Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}

[Unfilled bindings file](../bindings/n086.bindings.example.json). All binding values intentionally start as null.

## Pipeline and result semantics

Read the stages in order: the source and early filters establish the population; alter creates explicitly typed aliases; joins/union introduce related evidence; comp/windowcomp/dedup change row cardinality where present; the final projection, sort and limit define displayed rows. A result limit does not bound scanning or join work. Check the identity and time predicates in each subquery against the binding contract.

Estimated cost: MEDIUM. Bounded lookback and early filters; result caps do not bound upstream scanning. No measured performance claim.

## Limitations and tuning

- Endpoints with an empty latest snapshot have zero application rows by design; use the supplied snapshot-state inventory to distinguish empty from absent collection.
- One software row can expand across multiple group memberships. Snapshot/group applicability and identity joins must be validated; installed reports do not prove execution.
- No tenant compilation or execution. Validate nulls, access, retention and source collection before interpreting empty results.
- A terminal limit bounds displayed output, not upstream scans or join multiplicity.

## Acceptance scenarios — NOT RUN

- {"kind": "positive", "fixture": "Latest snapshot has unlisted ordinary software with a vendor/version and two groups", "expected": "Software appears in both group contexts even though not in a tool taxonomy."}
- {"kind": "negative", "fixture": "Older report contains software removed in a newer nonempty report", "expected": "Older software rows excluded."}
- {"kind": "null", "fixture": "Latest nonempty software row has no vendor or group membership", "expected": "Application remains with null enrichment."}
- {"kind": "edge", "fixture": "Latest application snapshot is explicitly empty, while a prior report had software", "expected": "No stale application rows are returned."}

These scenarios are plans for representative source fixtures and bounded tenant runs. They have not been executed in Cortex. First inspect the bound schema, then compile the complete body, then exercise positive, benign, negative, null and boundary cases. Do not promote this interactive hunt directly to BIOC/correlation without reviewing that surface.

Independent static review: **PASS**. Full-query C/E: **NOT RUN / NOT RUN**. Static review cannot establish source population or detection effectiveness.

## ATT&CK context

N/A — operational investigation

Operational visibility does not imply adversary behavior.

## References

- [Official source for OP-016](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields)
- [Official source for OP-016](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/action-actor)
- [Official source for OP-016](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/actor-actor)
- [Official source for OP-016](https://cortex-docs.paloaltonetworks.com/xql-command-reference-guide/readme/functions/timestamp_diff)
- [Official source for OP-016](https://docs-cortex.paloaltonetworks.com/r/Cortex/Cortex-XQL-Command-Reference/Example-2-Left-join?contentId=5iT66RH4ney5McWya7JKjQ)
- [Official source for OP-016](https://cortex-docs.paloaltonetworks.com/cortex-xsiam/reference-and-developer-docs/cortex-agentix-xql/get-started-with-xql/datasets-and-presets)
