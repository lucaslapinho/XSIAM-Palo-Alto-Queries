# N058 — CloudTrail StopLogging Requests and Observed Trail State - Schema Binding Template

Reviews AWS StopLogging requests and attaches trail-state observations within 15 minutes, preserving API errors and unknown state rather than assuming a successful logging shutdown.

Proposal: **TH-034**. Category: **Threat Hunting / Defense Impairment**.

Artifact: **SCHEMA_BINDING_TEMPLATE**. Product/surface: Cortex XSIAM / Interactive XQL Search. Tenant release, full-query compiler acceptance and execution: **UNVERIFIED / NOT RUN**.

[Query file](../queries/threat-hunting/defense-impairment/n058-cloudtrail-stoplogging-requests-and-observed-trail-state-schema-binding-template.xql)

## Objective and interpretation

One StopLogging request and each matching post-request status observation, or an unmatched request; AWS-specific slice of cloud logging disablement.

Output grain: One StopLogging request and each matching post-request status observation, or an unmatched request; AWS-specific slice of cloud logging disablement.
Timeframe: 1d unless explicitly expanded in body

## Source and field contract

Sources: `["{{AWS_CLOUDTRAIL_DATASET}}", "{{TRAIL_STATUS_SNAPSHOTS}}"]`.

Every schema token must resolve to an inspected field, expression or source with the stated type and meaning. A normalized source contract may require telemetry ingestion, parsing or a separately reviewed view; replacing names alone cannot create missing evidence. Preserve nulls. Verify timezone, identity namespaces, provider/channel, event outcomes, source completeness and row multiplicity.

- **AWS_CLOUDTRAIL_DATASET** — {"type": "DATASET_IDENTIFIER", "kind": "DATASET", "binding_contract": "Ingested CloudTrail management events; bind actual parser fields from original records, including operation and errors.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CT_EVENT_TIME** — {"type": "DATETIME", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "CloudTrail eventTime.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CT_EVENT_UID** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "CloudTrail eventID. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CT_ACCOUNT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Recipient account ID. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CT_REGION** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Event awsRegion. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CT_EVENT_SOURCE** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "CloudTrail eventSource. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CT_EVENT_NAME** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "CloudTrail eventName. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CT_TRAIL_ARN** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Resolve request trail Name/name-or-ARN to canonical account/region trail ARN; unresolved names remain null. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CT_ACTOR_SESSION** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Principal/session identifier with issuer context; do not expose credentials. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CT_SOURCE_ADDRESS** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "sourceIPAddress, preserving AWS-service attribution. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CT_ERROR_CODE** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Original errorCode; map empty to null only when source contract establishes absence. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **TRAIL_STATUS_SNAPSHOTS** — {"type": "DATASET_IDENTIFIER", "kind": "DATASET", "binding_contract": "Authenticated read-only historical GetTrailStatus/configuration snapshots with collection timestamp, including deleted/not-found state distinctly.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **TRAIL_STATUS_TIME** — {"type": "DATETIME", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Actual observation time, not query ingestion time.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **TRAIL_STATUS_ARN** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Canonical trail ARN matching request. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **TRAIL_STATUS_ACCOUNT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Account scope. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **TRAIL_IS_LOGGING** — {"type": "BOOLEAN", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Observed IsLogging; unknown/deleted/not-found must remain null with separate source context.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}

[Unfilled bindings file](../bindings/n058.bindings.example.json). All binding values intentionally start as null.

## Pipeline and result semantics

Read the stages in order: the source and early filters establish the population; alter creates explicitly typed aliases; joins/union introduce related evidence; comp/windowcomp/dedup change row cardinality where present; the final projection, sort and limit define displayed rows. A result limit does not bound scanning or join work. Check the identity and time predicates in each subquery against the binding contract.

Estimated cost: HIGH. Bounded lookback and early filters; result caps do not bound upstream scanning. No measured performance claim.

## Limitations and tuning

- No-error API metadata alone is not complete operational proof. Concurrent changes can alter later snapshots.
- One stopped trail does not establish loss of account/region logging coverage; other trails, event data stores and selectors require separate coverage evaluation.
- Other AWS logging-reduction operations and other cloud providers require separately verified operation semantics; not covered by this StopLogging scope.
- All bindings are explicit contracts, not claims that a source or field exists. Bind field/expression placeholders to typed tenant fields or tested extraction expressions; never substitute a made-up name.
- One-day telemetry lookback unless stated otherwise. Source-clock, retention, collection and permission gaps can remove evidence; _time filtering must cover each source event time.
- All canonical normalized words in expressions are local mapping contracts, not undocumented Cortex enum values.
- Result limits apply after analysis and do not cap scanned data or join fan-out. No tenant compile, execution, performance or deployment claim.
- ATT&CK, where present, is an analytical interpretation requiring context, not proof of compromise.
- Inventory/coverage/snapshot source bindings must return every relevant validity record under the configured query timeframe; if their storage timestamps differ, establish and test an explicit inner timeframe before use. A missing lookup row is never automatically benign.
- Comparisons are case-sensitive. Binding owners must canonicalize values only in source namespaces proven case-insensitive (such as reviewed Windows service/path/account keys and DNS A-label names), identically on both join sides. Preserve Unix paths, case-sensitive identities, ARN resource components, and source evidence text. All normalized operation/result labels must match the documented local contract spelling exactly.
- Time-difference bounds are expressed at second resolution. Validate subsecond boundary behavior using source fixtures; direct timestamp comparisons enforce before/after ordering where required.

## Acceptance scenarios — NOT RUN

- {"kind": "positive", "fixture": "StopLogging for trail A has a same-account status snapshot five minutes later with IsLogging false.", "expected": "Request shows OBSERVED_NOT_LOGGING.", "status": "NOT RUN"}
- {"kind": "negative", "fixture": "A DescribeTrails API operation occurs, or status belongs to another trail/account.", "expected": "Operation excluded; unrelated state never attaches.", "status": "NOT RUN"}
- {"kind": "null", "fixture": "Trail ARN cannot be resolved or no status snapshot exists.", "expected": "Request retained with UNKNOWN state.", "status": "NOT RUN"}
- {"kind": "edge", "fixture": "API returns AccessDenied while a concurrent administrator later stops the trail.", "expected": "Error is preserved; later observed state is not attributed as success of the failed request.", "status": "NOT RUN"}

These scenarios are plans for representative source fixtures and bounded tenant runs. They have not been executed in Cortex. First inspect the bound schema, then compile the complete body, then exercise positive, benign, negative, null and boundary cases. Do not promote this interactive hunt directly to BIOC/correlation without reviewing that surface.

Independent static review: **PASS**. Full-query C/E: **NOT RUN / NOT RUN**. Static review cannot establish source population or detection effectiveness.

## ATT&CK context

{"id": "T1685.002", "technique": "Disable or Modify Cloud Log", "tactics": "Defense Impairment (TA0112)"}

Source workbook ATT&CK Enterprise v19.2 hypothesis retained for traceability. The query observes only the behavior in its coverage boundary; mapping is not certified detection coverage.

## References

- [AWS StopLogging](https://docs.aws.amazon.com/awscloudtrail/latest/APIReference/API_StopLogging.html)
- [CloudTrail record contents](https://docs.aws.amazon.com/awscloudtrail/latest/userguide/cloudtrail-event-reference-record-contents.html)
- [MITRE Disable or Modify Tools](https://attack.mitre.org/techniques/T1685/)
- [Cortex XSIAM comp](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/comp?contentId=6TGIbW601nVvh2ycedN_hw)
- [Cortex XSIAM join](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/join?contentId=MyWFbdks6xgkqm_1lNkfGg)
- [Cortex XSIAM bin](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/bin?contentId=CU_u_bTkg_MICr5pUKivwg)
- [Cortex XSIAM sort](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/sort?contentId=a9tB71nqIWhA~Adcae6R7w)
