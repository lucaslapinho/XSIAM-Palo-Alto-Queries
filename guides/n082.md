# N082 — Investigation - Selected Process with Direct Parent and Children

Returns a selected process instance, its verified direct parent and direct child starts on one endpoint, preserving causality as separate context.

Proposal: **OP-012**. Category: **Investigation / Process Relationships**.

Artifact: **SCHEMA_BINDING_TEMPLATE**. Product/surface: Cortex XSIAM / Interactive XQL Search. Tenant release, full-query compiler acceptance and execution: **UNVERIFIED / NOT RUN**.

[Query file](../queries/investigation/process-relationships/n082-investigation-selected-process-with-direct-parent-and-children.xql)

## Objective and interpretation

Complete one-generation parent/child pivot template. Causality is displayed but not reclassified as direct ancestry; arbitrary descendant recursion is outside this selected-instance view.

Output grain: See the query projection and aggregation.
Timeframe: 7d

## Source and field contract

Sources: `["{{PROCESS_RELATIONSHIPS_DATASET}}"]`.

Every schema token must resolve to an inspected field, expression or source with the stated type and meaning. A normalized source contract may require telemetry ingestion, parsing or a separately reviewed view; replacing names alone cannot create missing evidence. Preserve nulls. Verify timezone, identity namespaces, provider/channel, event outcomes, source completeness and row multiplicity.

- **PROCESS_RELATIONSHIPS_DATASET** — {"type": "XQL dataset identifier", "description": "Actual read-only source for PROCESS_RELATIONSHIPS. Bind a documented/inspected dataset or reviewed view; this name is not a claim of an existing integration.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **PROCESS_RELATIONSHIPS_EVENT_TIME** — {"type": "DATETIME scalar expression", "description": "Process start time Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **PROCESS_RELATIONSHIPS_HOST_ID** — {"type": "STRING scalar expression", "description": "Stable endpoint identity Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **PROCESS_RELATIONSHIPS_PROCESS_ID** — {"type": "STRING scalar expression", "description": "Stable process-instance key, never PID alone Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **PROCESS_RELATIONSHIPS_PARENT_ID** — {"type": "STRING scalar expression", "description": "Verified immediate-parent instance key Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **PROCESS_RELATIONSHIPS_CAUSALITY_ID** — {"type": "STRING scalar expression", "description": "Causality group identifier with documented scope; null when unavailable Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **PROCESS_RELATIONSHIPS_IMAGE** — {"type": "STRING scalar expression", "description": "Started image path Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **PROCESS_RELATIONSHIPS_COMMAND_LINE** — {"type": "STRING scalar expression", "description": "Started command line Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **PROCESS_RELATIONSHIPS_ACCOUNT** — {"type": "STRING scalar expression", "description": "Started account Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **SELECTED_ENDPOINT_ID** — {"type": "STRING literal", "description": "Quoted stable endpoint ID; no empty/all-host fallback", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **SELECTED_PROCESS_INSTANCE** — {"type": "STRING literal", "description": "Quoted stable process-instance ID; numeric PID alone is not accepted", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}

[Unfilled bindings file](../bindings/n082.bindings.example.json). All binding values intentionally start as null.

## Pipeline and result semantics

Read the stages in order: the source and early filters establish the population; alter creates explicitly typed aliases; joins/union introduce related evidence; comp/windowcomp/dedup change row cardinality where present; the final projection, sort and limit define displayed rows. A result limit does not bound scanning or join work. Check the identity and time predicates in each subquery against the binding contract.

Estimated cost: MEDIUM. Bounded lookback and early filters; result caps do not bound upstream scanning. No measured performance claim.

## Limitations and tuning

- Parent and selected starts predating the seven-day scan may be absent; process lifetime/current state is not inferred.
- Stable process IDs and verified parent linkage must use a common host/boot namespace; validate process-provider replay/duplicate behavior.
- No tenant compilation or execution. Validate nulls, access, retention and source collection before interpreting empty results.
- A terminal limit bounds displayed output, not upstream scans or join multiplicity.

## Acceptance scenarios — NOT RUN

- {"kind": "positive", "fixture": "Selected stable instance P has parent A and children B/C", "expected": "SELECTED_PROCESS, DIRECT_PARENT and both DIRECT_CHILD rows."}
- {"kind": "negative", "fixture": "Numeric PID reused by a different instance or another host", "expected": "No false relationship."}
- {"kind": "null", "fixture": "Parent instance missing or outside retention", "expected": "Selected/child rows remain; no guessed parent."}
- {"kind": "edge", "fixture": "Causality owner differs from direct parent", "expected": "Only verified parent is DIRECT_PARENT; causality remains context."}

These scenarios are plans for representative source fixtures and bounded tenant runs. They have not been executed in Cortex. First inspect the bound schema, then compile the complete body, then exercise positive, benign, negative, null and boundary cases. Do not promote this interactive hunt directly to BIOC/correlation without reviewing that surface.

Independent static review: **PASS**. Full-query C/E: **NOT RUN / NOT RUN**. Static review cannot establish source population or detection effectiveness.

## ATT&CK context

N/A — operational investigation

Operational visibility does not imply adversary behavior.

## References

- [Official source for OP-012](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields)
- [Official source for OP-012](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/action-actor)
- [Official source for OP-012](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/actor-actor)
- [Official source for OP-012](https://cortex-docs.paloaltonetworks.com/xql-command-reference-guide/readme/functions/timestamp_diff)
- [Official source for OP-012](https://docs-cortex.paloaltonetworks.com/r/Cortex/Cortex-XQL-Command-Reference/Example-2-Left-join?contentId=5iT66RH4ney5McWya7JKjQ)
- [Official source for OP-012](https://docs-cortex.paloaltonetworks.com/r/Cortex/Cortex-XQL-Command-Reference/Example-2-Union-with-an-Inner-XQL-Query)
- [Official source for OP-012](https://learn.microsoft.com/en-us/sysinternals/downloads/sysmon)
