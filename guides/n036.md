# N036 — Threat Hunting - New Domain Accounts and Early Lifecycle Activity

Preserves verified domain-account creation and adds first observed successful authentication plus subsequent group additions within 24 hours using stable SID/domain keys.

Proposal: **TH-011**. Category: **Threat Hunting / Identity**.

Artifact: **SCHEMA_BINDING_TEMPLATE**. Product/surface: Cortex XSIAM / Interactive XQL Search. Tenant release, full-query compiler acceptance and execution: **UNVERIFIED / NOT RUN**.

[Query file](../queries/threat-hunting/identity/n036-threat-hunting-new-domain-accounts-and-early-lifecycle-activity.xql)

## Objective and interpretation

Full creation-to-authentication-to-membership template. First means observed after creation inside the selected retention/window, not first-ever activity.

Output grain: See the query projection and aggregation.
Timeframe: 3d

## Source and field contract

Sources: `["{{ACCOUNT_LIFECYCLE_EVENTS_DATASET}}", "{{DOMAIN_ACCOUNT_CREATION_DATASET}}"]`.

Every schema token must resolve to an inspected field, expression or source with the stated type and meaning. A normalized source contract may require telemetry ingestion, parsing or a separately reviewed view; replacing names alone cannot create missing evidence. Preserve nulls. Verify timezone, identity namespaces, provider/channel, event outcomes, source completeness and row multiplicity.

- **DOMAIN_ACCOUNT_CREATION_DATASET** — {"type": "XQL dataset identifier", "description": "Actual read-only source for DOMAIN_ACCOUNT_CREATION. Bind a documented/inspected dataset or reviewed view; this name is not a claim of an existing integration.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **DOMAIN_ACCOUNT_CREATION_CREATION_TIME** — {"type": "DATETIME scalar expression", "description": "4720 creation time Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **DOMAIN_ACCOUNT_CREATION_ACCOUNT_SID** — {"type": "STRING scalar expression", "description": "Created TargetSid Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **DOMAIN_ACCOUNT_CREATION_DOMAIN_ID** — {"type": "STRING scalar expression", "description": "Authoritative domain SID/identifier; cannot be inferred from host/account name Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **DOMAIN_ACCOUNT_CREATION_CREATOR_SID** — {"type": "STRING scalar expression", "description": "Creating subject SID Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **DOMAIN_ACCOUNT_CREATION_CREATION_HOST** — {"type": "STRING scalar expression", "description": "Auditing host Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **DOMAIN_ACCOUNT_CREATION_CREATED_NAME** — {"type": "STRING scalar expression", "description": "Created account name Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **DOMAIN_ACCOUNT_CREATION_EVENT_ID** — {"type": "INTEGER scalar expression", "description": "Provider-verified event ID Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **DOMAIN_ACCOUNT_CREATION_IS_DOMAIN_ACCOUNT** — {"type": "BOOLEAN scalar expression", "description": "Verified domain-account classification from DC/domain inventory and TargetSid, excluding local accounts Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **ACCOUNT_LIFECYCLE_EVENTS_DATASET** — {"type": "XQL dataset identifier", "description": "Actual read-only source for ACCOUNT_LIFECYCLE_EVENTS. Bind a documented/inspected dataset or reviewed view; this name is not a claim of an existing integration.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **ACCOUNT_LIFECYCLE_EVENTS_L_SID** — {"type": "STRING scalar expression", "description": "Target authenticated SID or added MemberSid Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **ACCOUNT_LIFECYCLE_EVENTS_L_DOMAIN** — {"type": "STRING scalar expression", "description": "Same authoritative domain namespace Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **ACCOUNT_LIFECYCLE_EVENTS_L_TIME** — {"type": "DATETIME scalar expression", "description": "Later activity time Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **ACCOUNT_LIFECYCLE_EVENTS_L_KIND** — {"type": "STRING scalar expression", "description": "Normalized SUCCESSFUL_AUTH or GROUP_ADDITION from actual outcome/event family Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **ACCOUNT_LIFECYCLE_EVENTS_L_HOST** — {"type": "STRING scalar expression", "description": "Authentication/group-change target host Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **ACCOUNT_LIFECYCLE_EVENTS_L_DETAIL** — {"type": "STRING scalar expression", "description": "Provider/outcome/source context or target group SID and role Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}

[Unfilled bindings file](../bindings/n036.bindings.example.json). All binding values intentionally start as null.

## Pipeline and result semantics

Read the stages in order: the source and early filters establish the population; alter creates explicitly typed aliases; joins/union introduce related evidence; comp/windowcomp/dedup change row cardinality where present; the final projection, sort and limit define displayed rows. A result limit does not bound scanning or join work. Check the identity and time predicates in each subquery against the binding contract.

Estimated cost: MEDIUM. Bounded lookback and early filters; result caps do not bound upstream scanning. No measured performance claim.

## Limitations and tuning

- 4720 on a member/workstation can create a local account and must not pass the domain-account binding.
- A three-day scan supplies event context; creation near the scan end has incomplete future follow-up. Group additions are privilege context only after target-group review.
- No tenant compilation or execution. Validate nulls, access, retention and source collection before interpreting empty results.
- A terminal limit bounds displayed output, not upstream scans or join multiplicity.

## Acceptance scenarios — NOT RUN

- {"kind": "positive", "fixture": "Verified domain account created at T, authenticates at T+10 and T+20 minutes, then joins a group", "expected": "Creation, first-auth at +10 and group-add evidence."}
- {"kind": "negative", "fixture": "Local workstation account with 4720 or a different domain reuses the display name", "expected": "Excluded or not correlated."}
- {"kind": "null", "fixture": "Created SID has no subsequent telemetry", "expected": "Creation remains without fabricated authentication."}
- {"kind": "edge", "fixture": "Group addition at T-1 or T+86401 seconds", "expected": "Excluded from subsequent lifecycle evidence."}

These scenarios are plans for representative source fixtures and bounded tenant runs. They have not been executed in Cortex. First inspect the bound schema, then compile the complete body, then exercise positive, benign, negative, null and boundary cases. Do not promote this interactive hunt directly to BIOC/correlation without reviewing that surface.

Independent static review: **PASS**. Full-query C/E: **NOT RUN / NOT RUN**. Static review cannot establish source population or detection effectiveness.

## ATT&CK context

{"id": "T1136.002", "technique": "Domain Account", "tactics": "Persistence (TA0003)"}

Source workbook ATT&CK Enterprise v19.2 hypothesis retained for traceability. The query observes only the behavior in its coverage boundary; mapping is not certified detection coverage.

## References

- [Official source for TH-011](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields)
- [Official source for TH-011](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/action-actor)
- [Official source for TH-011](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/actor-actor)
- [Official source for TH-011](https://cortex-docs.paloaltonetworks.com/xql-command-reference-guide/readme/functions/timestamp_diff)
- [Official source for TH-011](https://docs-cortex.paloaltonetworks.com/r/Cortex/Cortex-XQL-Command-Reference/Example-2-Left-join?contentId=5iT66RH4ney5McWya7JKjQ)
- [Official source for TH-011](https://docs-cortex.paloaltonetworks.com/r/Cortex/Cortex-XQL-Command-Reference/Example-2-Union-with-an-Inner-XQL-Query)
- [Official source for TH-011](https://learn.microsoft.com/en-us/previous-versions/windows/it-pro/windows-10/security/threat-protection/auditing/event-4720)
- [Official source for TH-011](https://learn.microsoft.com/en-us/previous-versions/windows/it-pro/windows-10/security/threat-protection/auditing/event-4624)
- [Official source for TH-011](https://attack.mitre.org/techniques/T1136/002/)
