# N025 — Dashboard - Outbound Destination Ports - Network Event Volume by Endpoint

Ranks outgoing endpoint-associated TCP and UDP network event rows by stable endpoint ID, IP protocol, remote port and operation subtype over one day. Keeps hostnames as context and excludes unknown direction or invalid ports; counts are event volume, not unique connections or identified applications.

Proposal: **OP-008**. Category: **Dashboard / Network Activity**.

Artifact: **QUERY_CANDIDATE**. Product/surface: Cortex XSIAM / XQL Search. Tenant release, full-query compiler acceptance and execution: **UNVERIFIED / NOT RUN**.

[Query file](../queries/dashboard/network-activity/n025-dashboard-outbound-destination-ports-network-event-volume-by-endpoint.xql)

## Objective and interpretation

Ranks outgoing endpoint-associated TCP and UDP network event rows by stable endpoint ID, IP protocol, remote port and operation subtype over one day. Keeps hostnames as context and excludes unknown direction or invalid ports; counts are event volume, not unique connections or identified applications.

Output grain: One group per agent_id, IP protocol, remote port and operation subtype; metric is count of admitted rows.
Timeframe: 1d

## Source and field contract

Sources: `["xdr_data"]`.

Every schema token must resolve to an inspected field, expression or source with the stated type and meaning. A normalized source contract may require telemetry ingestion, parsing or a separately reviewed view; replacing names alone cannot create missing evidence. Preserve nulls. Verify timezone, identity namespaces, provider/channel, event outcomes, source completeness and row multiplicity.

No schema binding tokens. Review the actual field population, timeframe and source access before editor use.

## Pipeline and result semantics

Read the stages in order: the source and early filters establish the population; alter creates explicitly typed aliases; joins/union introduce related evidence; comp/windowcomp/dedup change row cardinality where present; the final projection, sort and limit define displayed rows. A result limit does not bound scanning or join work. Check the identity and time predicates in each subquery against the binding contract.

Estimated cost: MEDIUM. Explicit one-day scope and early filters reduce the admitted population; no performance measurements. A final limit caps returned rows, not scan cost.

## Limitations and tuning

- This proposal is implemented as destination-port visibility; App-ID ranking requires separately verified application telemetry.
- For outgoing rows the remote port is the destination port; incoming rows are deliberately excluded.
- Hostname changes do not split stable endpoint groups; values preserves distinct observed labels.
- No connection-ID uniqueness or session-start subtype is assumed.

## Acceptance scenarios — NOT RUN


These scenarios are plans for representative source fixtures and bounded tenant runs. They have not been executed in Cortex. First inspect the bound schema, then compile the complete body, then exercise positive, benign, negative, null and boundary cases. Do not promote this interactive hunt directly to BIOC/correlation without reviewing that surface.

Independent static review: **PASS**. Full-query C/E: **NOT RUN / NOT RUN**. Static review cannot establish source population or detection effectiveness.

## ATT&CK context

N/A

Contextual hypothesis only; this is not certified technique coverage.

## References


See the retained [first-six source register](../FIRST-SIX-REFERENCES.md), [validation boundaries](../FIRST-SIX-VALIDATION.md), and [detailed query guide](../FIRST-SIX-GUIDE.md).

The inspected Schema reports action_network_protocol as ENUM while the official reference describes INTEGER/IPPROTO. Numeric 6/17 comparisons remain a compiler-validation gap; no full-body acceptance is claimed.
