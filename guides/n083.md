# N083 — Investigation - Selected SHA-256 Process and File Prevalence

Summarizes a selected SHA-256 across process starts and file-event observations over 30 days, retaining separate evidence-family counts, distinct endpoints, paths/users and first/last observations.

Proposal: **OP-013**. Category: **Investigation / File Identity**.

Artifact: **QUERY_CANDIDATE**. Product/surface: Cortex XSIAM / Interactive XQL Search. Tenant release, full-query compiler acceptance and execution: **UNVERIFIED / NOT RUN**.

[Query file](../queries/investigation/file-identity/n083-investigation-selected-sha-256-process-and-file-prevalence.xql)

## Objective and interpretation

Both process and file branches are implemented with documented SHA-256 fields. Family separation prevents a combined event count from masquerading as unique execution prevalence.

Output grain: See the query projection and aggregation.
Timeframe: 30d

## Source and field contract

Sources: `["xdr_data"]`.

Every schema token must resolve to an inspected field, expression or source with the stated type and meaning. A normalized source contract may require telemetry ingestion, parsing or a separately reviewed view; replacing names alone cannot create missing evidence. Preserve nulls. Verify timezone, identity namespaces, provider/channel, event outcomes, source completeness and row multiplicity.

- **sha256_to_review** — {"type": "STRING value", "required": true, "description": "Replace SHA256_TO_REVIEW with a 64-hex SHA-256 string; the unchanged sentinel matches no valid hash."}

## Pipeline and result semantics

Read the stages in order: the source and early filters establish the population; alter creates explicitly typed aliases; joins/union introduce related evidence; comp/windowcomp/dedup change row cardinality where present; the final projection, sort and limit define displayed rows. A result limit does not bound scanning or join work. Check the identity and time predicates in each subquery against the binding contract.

Estimated cost: MEDIUM. Bounded lookback and early filters; result caps do not bound upstream scanning. No measured performance claim.

## Limitations and tuning

- File events are hash-bearing observations, not a complete installed/current-file inventory; operations may repeat.
- Counts are retained rows per family. First/last are observation bounds, not first-ever use; array contexts can be large.
- FILE symbolic acceptance and all projected field population remain tenant-unverified.
- No tenant compilation or execution. Validate nulls, access, retention and source collection before interpreting empty results.
- A terminal limit bounds displayed output, not upstream scans or join multiplicity.

## Acceptance scenarios — NOT RUN

- {"kind": "positive", "fixture": "Same selected hash appears in process starts on two endpoints and file events on three", "expected": "Separate family rows with prevalence 2 and 3."}
- {"kind": "negative", "fixture": "Other hashes, malformed hashes and empty endpoint IDs", "expected": "Excluded."}
- {"kind": "null", "fixture": "File event lacks SHA-256 or process account", "expected": "Null hash excluded; null account does not remove valid hash evidence."}
- {"kind": "edge", "fixture": "Same endpoint repeats a hash across paths and many operations", "expected": "Event count increases; distinct endpoint count does not."}

These scenarios are plans for representative source fixtures and bounded tenant runs. They have not been executed in Cortex. First inspect the bound schema, then compile the complete body, then exercise positive, benign, negative, null and boundary cases. Do not promote this interactive hunt directly to BIOC/correlation without reviewing that surface.

Independent static review: **PASS**. Full-query C/E: **NOT RUN / NOT RUN**. Static review cannot establish source population or detection effectiveness.

## ATT&CK context

N/A — operational investigation

Operational visibility does not imply adversary behavior.

## References

- [Official source for OP-013](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields)
- [Official source for OP-013](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/action-actor)
- [Official source for OP-013](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/actor-actor)
- [Official source for OP-013](https://cortex-docs.paloaltonetworks.com/xql-command-reference-guide/readme/functions/timestamp_diff)
- [Official source for OP-013](https://docs-cortex.paloaltonetworks.com/r/Cortex/Cortex-XQL-Command-Reference/Example-2-Union-with-an-Inner-XQL-Query)
- [Cortex XDR union: process-start and file-write example](https://docs-cortex.paloaltonetworks.com/r/Cortex-XDR/Cortex-XDR-3.x-Documentation/union)
- [XQL filter Example 22: ordinary double-quoted strings](https://docs-cortex.paloaltonetworks.com/r/Cortex/Cortex-XQL-Command-Reference/Example-22-String-manipulation-single-quotes?contentId=lwRG5fwKl6gNfg7ngRAAMA)
- [RE2 syntax reference](https://github.com/google/re2/wiki/syntax)
