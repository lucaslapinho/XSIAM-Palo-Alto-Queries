# N057 — Windows Event Log Clearing Records - Schema Binding Template

Returns verified log-clearing audit events with the actual cleared channel and correctly parsed subject identity. The provider/event allowlist keeps confirmed clearing separate from attempted commands.

Proposal: **TH-033**. Category: **Threat Hunting / Defense Impairment**.

Artifact: **SCHEMA_BINDING_TEMPLATE**. Product/surface: Cortex XSIAM / Interactive XQL Search. Tenant release, full-query compiler acceptance and execution: **UNVERIFIED / NOT RUN**.

[Query file](../queries/threat-hunting/defense-impairment/n057-windows-event-log-clearing-records-schema-binding-template.xql)

## Objective and interpretation

One collected clearing event for a specifically identified Windows log channel; source allowlist defines coverage.

Output grain: One collected clearing event for a specifically identified Windows log channel; source allowlist defines coverage.
Timeframe: 1d unless explicitly expanded in body

## Source and field contract

Sources: `["{{WINDOWS_LOG_CLEAR_DATASET}}"]`.

Every schema token must resolve to an inspected field, expression or source with the stated type and meaning. A normalized source contract may require telemetry ingestion, parsing or a separately reviewed view; replacing names alone cannot create missing evidence. Preserve nulls. Verify timezone, identity namespaces, provider/channel, event outcomes, source completeness and row multiplicity.

- **WINDOWS_LOG_CLEAR_DATASET** — {"type": "DATASET_IDENTIFIER", "kind": "DATASET", "binding_contract": "Collected Windows log-clear audit events with verified provider/channel/event semantics. Security 1102 UserData and other channel events require distinct tested mappings.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **LOG_CLEAR_TIME** — {"type": "DATETIME", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Original event time.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **LOG_CLEAR_UID** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Unique source event key. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **LOG_CLEAR_ENDPOINT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Stable endpoint. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **LOG_CLEAR_PROVIDER** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Original provider. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **LOG_CLEAR_CHANNEL** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Channel carrying this event. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **LOG_CLEAR_EVENT_ID** — {"type": "INTEGER", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Original EventID.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **LOG_CLEAR_TARGET_CHANNEL** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Actual channel cleared; for validated Security 1102 map Security, not all Windows logs. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **LOG_CLEAR_OPERATION** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "log_cleared only for source events confirming clearing; command_attempt differs. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **LOG_CLEAR_SUBJECT_SID** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "UserData SubjectUserSid for Security 1102 or equivalent verified subject mapping. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **LOG_CLEAR_SUBJECT_ACCOUNT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Realm-qualified subject from correct UserData/event schema. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **LOG_CLEAR_SUBJECT_LOGON** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Source SubjectLogonId with host/boot context; null when absent. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **VERIFIED_CLEAR_EVENT_PREDICATE** — {"type": "BOOLEAN_EXPRESSION", "kind": "VALUE_OR_LITERAL", "binding_contract": "Exact tested provider/channel/EventID allowlist over provider/channel/event_id; must include only documented clearing events such as Security 1102 and separately verified additional-channel events.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}

[Unfilled bindings file](../bindings/n057.bindings.example.json). All binding values intentionally start as null.

## Pipeline and result semantics

Read the stages in order: the source and early filters establish the population; alter creates explicitly typed aliases; joins/union introduce related evidence; comp/windowcomp/dedup change row cardinality where present; the final projection, sort and limit define displayed rows. A result limit does not bound scanning or join work. Check the identity and time predicates in each subquery against the binding contract.

Estimated cost: MEDIUM. Bounded lookback and early filters; result caps do not bound upstream scanning. No measured performance claim.

## Limitations and tuning

- Security 1102 alone covers Security clearing, not every Windows channel.
- 1102 identity comes from UserData; ordinary EventData extraction cannot be presumed equivalent.
- Central retention and preceding collection coverage require independent review; missing earlier events are not proof of local clearing.
- All bindings are explicit contracts, not claims that a source or field exists. Bind field/expression placeholders to typed tenant fields or tested extraction expressions; never substitute a made-up name.
- One-day telemetry lookback unless stated otherwise. Source-clock, retention, collection and permission gaps can remove evidence; _time filtering must cover each source event time.
- All canonical normalized words in expressions are local mapping contracts, not undocumented Cortex enum values.
- Result limits apply after analysis and do not cap scanned data or join fan-out. No tenant compile, execution, performance or deployment claim.
- ATT&CK, where present, is an analytical interpretation requiring context, not proof of compromise.
- Inventory/coverage/snapshot source bindings must return every relevant validity record under the configured query timeframe; if their storage timestamps differ, establish and test an explicit inner timeframe before use. A missing lookup row is never automatically benign.
- Comparisons are case-sensitive. Binding owners must canonicalize values only in source namespaces proven case-insensitive (such as reviewed Windows service/path/account keys and DNS A-label names), identically on both join sides. Preserve Unix paths, case-sensitive identities, ARN resource components, and source evidence text. All normalized operation/result labels must match the documented local contract spelling exactly.
- Time-difference bounds are expressed at second resolution. Validate subsecond boundary behavior using source fixtures; direct timestamp comparisons enforce before/after ordering where required.

## Acceptance scenarios — NOT RUN

- {"kind": "positive", "fixture": "A valid Security 1102 with UserData Subject identity names Security as the cleared channel.", "expected": "Returned with subject mapping.", "status": "NOT RUN"}
- {"kind": "negative", "fixture": "wevtutil command starts but no confirming clear event exists.", "expected": "Excluded from this confirmed-event view.", "status": "NOT RUN"}
- {"kind": "null", "fixture": "Subject SID is absent but clearing channel/event semantics are verified.", "expected": "Event remains with null actor context.", "status": "NOT RUN"}
- {"kind": "edge", "fixture": "EventID 1102 from an unrelated provider or an unverified channel is present.", "expected": "Excluded by the exact provider/channel predicate.", "status": "NOT RUN"}

These scenarios are plans for representative source fixtures and bounded tenant runs. They have not been executed in Cortex. First inspect the bound schema, then compile the complete body, then exercise positive, benign, negative, null and boundary cases. Do not promote this interactive hunt directly to BIOC/correlation without reviewing that surface.

Independent static review: **PASS**. Full-query C/E: **NOT RUN / NOT RUN**. Static review cannot establish source population or detection effectiveness.

## ATT&CK context

{"id": "T1685.005", "technique": "Clear Windows Event Logs", "tactics": "Defense Impairment (TA0112)"}

Source workbook ATT&CK Enterprise v19.2 hypothesis retained for traceability. The query observes only the behavior in its coverage boundary; mapping is not certified detection coverage.

## References

- [Microsoft event 1102](https://learn.microsoft.com/en-us/previous-versions/windows/it-pro/windows-10/security/threat-protection/auditing/event-1102)
- [MITRE Disable or Modify Tools](https://attack.mitre.org/techniques/T1685/)
- [Cortex XSIAM comp](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/comp?contentId=6TGIbW601nVvh2ycedN_hw)
- [Cortex XSIAM join](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/join?contentId=MyWFbdks6xgkqm_1lNkfGg)
- [Cortex XSIAM bin](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/bin?contentId=CU_u_bTkg_MICr5pUKivwg)
- [Cortex XSIAM sort](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/sort?contentId=a9tB71nqIWhA~Adcae6R7w)
