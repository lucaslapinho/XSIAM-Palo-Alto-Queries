# N055 — SSH Accepted Access and Session Command Review - Schema Binding Template

Preserves accepted SSH authentications and attaches audited exec events through a validated server-session key, showing missing command evidence explicitly.

Proposal: **TH-031**. Category: **Threat Hunting / Lateral Movement**.

Artifact: **SCHEMA_BINDING_TEMPLATE**. Product/surface: Cortex XSIAM / Interactive XQL Search. Tenant release, full-query compiler acceptance and execution: **UNVERIFIED / NOT RUN**.

[Query file](../queries/threat-hunting/lateral-movement/n055-ssh-accepted-access-and-session-command-review-schema-binding-template.xql)

## Objective and interpretation

One authentication/linked-exec pair, or one authentication with no linked exec within one hour; left join keeps unmatched authentications.

Output grain: One authentication/linked-exec pair, or one authentication with no linked exec within one hour; left join keeps unmatched authentications.
Timeframe: 1d unless explicitly expanded in body

## Source and field contract

Sources: `["{{SSH_AUTH_DATASET}}", "{{SSH_COMMAND_AUDIT_DATASET}}"]`.

Every schema token must resolve to an inspected field, expression or source with the stated type and meaning. A normalized source contract may require telemetry ingestion, parsing or a separately reviewed view; replacing names alone cannot create missing evidence. Preserve nulls. Verify timezone, identity namespaces, provider/channel, event outcomes, source completeness and row multiplicity.

- **SSH_AUTH_DATASET** — {"type": "DATASET_IDENTIFIER", "kind": "DATASET", "binding_contract": "Parsed successful SSH authentication with server-side session/audit linkage, supporting all collected accepted methods rather than publickey-only text.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SSH_AUTH_TIME** — {"type": "DATETIME", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Accepted authentication source time.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SSH_TARGET_ENDPOINT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Stable SSH server identity. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SSH_CLIENT_IP** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Canonical client address; preserve bastion/proxy interpretation. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SSH_AUTH_ACCOUNT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Authenticated account in target realm. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SSH_AUTH_METHOD** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Observed method such as publickey/password; unknown stays null. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SSH_SESSION_KEY** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Validated server/boot-scoped sshd child/session or audit login ID; no PID reuse ambiguity. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SSH_AUTH_RESULT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "success only for fully accepted authentication; partial success and failed attempts differ. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SSH_COMMAND_AUDIT_DATASET** — {"type": "DATASET_IDENTIFIER", "kind": "DATASET", "binding_contract": "OS exec audit tied to SSH login session with original login identity retained across sudo/su transitions.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SSH_COMMAND_TIME** — {"type": "DATETIME", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Audited exec timestamp.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SSH_COMMAND_ENDPOINT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Stable target server. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SSH_COMMAND_SESSION** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Same validated session key as authentication; audit login identity preserved. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SSH_COMMAND_UID** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Unique exec event key. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SSH_EFFECTIVE_USER** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Effective executing principal, distinct from authenticated account. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SSH_COMMAND_LINE** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Audited executable and safe arguments. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **SSH_COMMAND_OPERATION** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "exec only for actual audited execution, not shell-history text. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}

[Unfilled bindings file](../bindings/n055.bindings.example.json). All binding values intentionally start as null.

## Pipeline and result semantics

Read the stages in order: the source and early filters establish the population; alter creates explicitly typed aliases; joins/union introduce related evidence; comp/windowcomp/dedup change row cardinality where present; the final projection, sort and limit define displayed rows. A result limit does not bound scanning or join work. Check the identity and time predicates in each subquery against the binding contract.

Estimated cost: HIGH. Bounded lookback and early filters; result caps do not bound upstream scanning. No measured performance claim.

## Limitations and tuning

- No linked command does not mean no command ran; audit coverage and session mapping may be incomplete.
- Bastion/service accounts need explicit policy context; no blanket private-address or account exclusion.
- Authentication principal and effective command user may differ legitimately through elevation.
- All bindings are explicit contracts, not claims that a source or field exists. Bind field/expression placeholders to typed tenant fields or tested extraction expressions; never substitute a made-up name.
- One-day telemetry lookback unless stated otherwise. Source-clock, retention, collection and permission gaps can remove evidence; _time filtering must cover each source event time.
- All canonical normalized words in expressions are local mapping contracts, not undocumented Cortex enum values.
- Result limits apply after analysis and do not cap scanned data or join fan-out. No tenant compile, execution, performance or deployment claim.
- ATT&CK, where present, is an analytical interpretation requiring context, not proof of compromise.
- Inventory/coverage/snapshot source bindings must return every relevant validity record under the configured query timeframe; if their storage timestamps differ, establish and test an explicit inner timeframe before use. A missing lookup row is never automatically benign.
- Comparisons are case-sensitive. Binding owners must canonicalize values only in source namespaces proven case-insensitive (such as reviewed Windows service/path/account keys and DNS A-label names), identically on both join sides. Preserve Unix paths, case-sensitive identities, ARN resource components, and source evidence text. All normalized operation/result labels must match the documented local contract spelling exactly.
- Time-difference bounds are expressed at second resolution. Validate subsecond boundary behavior using source fixtures; direct timestamp comparisons enforce before/after ordering where required.

## Acceptance scenarios — NOT RUN

- {"kind": "positive", "fixture": "Accepted public-key authentication has an audited exec on the same server/session ten minutes later.", "expected": "SESSION_LINKED_EXEC returned with authenticated and effective users.", "status": "NOT RUN"}
- {"kind": "negative", "fixture": "Another session executes on the same server at the same time.", "expected": "Not attached to this authentication.", "status": "NOT RUN"}
- {"kind": "null", "fixture": "Authentication has valid session but no command audit.", "expected": "Authentication remains with NO_LINKED_EXEC_IN_WINDOW.", "status": "NOT RUN"}
- {"kind": "edge", "fixture": "sshd PID is reused after reboot.", "expected": "Boot-scoped key prevents cross-boot attachment.", "status": "NOT RUN"}

These scenarios are plans for representative source fixtures and bounded tenant runs. They have not been executed in Cortex. First inspect the bound schema, then compile the complete body, then exercise positive, benign, negative, null and boundary cases. Do not promote this interactive hunt directly to BIOC/correlation without reviewing that surface.

Independent static review: **PASS**. Full-query C/E: **NOT RUN / NOT RUN**. Static review cannot establish source population or detection effectiveness.

## ATT&CK context

{"id": "T1021.004", "technique": "SSH", "tactics": "Lateral Movement (TA0008)"}

Source workbook ATT&CK Enterprise v19.2 hypothesis retained for traceability. The query observes only the behavior in its coverage boundary; mapping is not certified detection coverage.

## References

- [OpenSSH manuals](https://www.openssh.org/manual.html)
- [Cortex XSIAM comp](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/comp?contentId=6TGIbW601nVvh2ycedN_hw)
- [Cortex XSIAM join](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/join?contentId=MyWFbdks6xgkqm_1lNkfGg)
- [Cortex XSIAM bin](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/bin?contentId=CU_u_bTkg_MICr5pUKivwg)
- [Cortex XSIAM sort](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/sort?contentId=a9tB71nqIWhA~Adcae6R7w)
