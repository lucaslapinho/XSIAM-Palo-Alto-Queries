# N027 — Threat Hunting - Office Command Shells and Subsequent Activity

Preserves verified Office-to-cmd process-start anchors and a ten-minute downstream command, download, write and network timeline after ancestry/source binding. Downloads require actual download evidence; a connection alone is not a download.

Proposal: **TH-002**. Category: **Threat Hunting / Execution**.

Artifact: **SCHEMA_BINDING_TEMPLATE**. Product/surface: Cortex XSIAM / Interactive XQL Search. Tenant release, full-query compiler acceptance and execution: **UNVERIFIED / NOT RUN**.

[Query file](../queries/threat-hunting/execution/n027-threat-hunting-office-command-shells-and-subsequent-activity.xql)

## Objective and interpretation

Full shell-plus-downstream workflow template. The ancestry bridge and activity kind mappings must cover the agreed descendant depth; no macro/phishing or successful-download inference from ancestry.

Output grain: See the query projection and aggregation.
Timeframe: 1d

## Source and field contract

Sources: `["xdr_data", "{{OFFICE_DESCENDANT_ACTIVITY_DATASET}}"]`.

Every schema token must resolve to an inspected field, expression or source with the stated type and meaning. A normalized source contract may require telemetry ingestion, parsing or a separately reviewed view; replacing names alone cannot create missing evidence. Preserve nulls. Verify timezone, identity namespaces, provider/channel, event outcomes, source completeness and row multiplicity.

- **VERIFIED_DIRECT_PARENT_IMAGE** — {"type": "STRING scalar expression", "description": "Immediate parent image from verified process relationship evidence, not an assumed actor/causality owner", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **OFFICE_DESCENDANT_ACTIVITY_DATASET** — {"type": "XQL dataset identifier", "description": "Actual read-only source for OFFICE_DESCENDANT_ACTIVITY. Bind a documented/inspected dataset or reviewed view; this name is not a claim of an existing integration.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **OFFICE_DESCENDANT_ACTIVITY_CTX_HOST** — {"type": "STRING scalar expression", "description": "Stable endpoint ID in the same namespace as the anchor Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **OFFICE_DESCENDANT_ACTIVITY_CTX_ROOT** — {"type": "STRING scalar expression", "description": "Root process instance for this bounded ancestry relation. Must relate the selected cmd process and its descendants to the same root with a documented maximum depth and stable IDs. A native actor or causality-owner field may only be used if that exact relationship is verified; otherwise bind a reviewed ancestry bridge/view. Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **OFFICE_DESCENDANT_ACTIVITY_CTX_TIME** — {"type": "DATETIME scalar expression", "description": "Original observed activity time Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **OFFICE_DESCENDANT_ACTIVITY_CTX_KIND** — {"type": "STRING scalar expression", "description": "Explicit normalized activity kind derived from verified event type/subtype, not a name-only inference Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **OFFICE_DESCENDANT_ACTIVITY_CTX_PROCESS** — {"type": "STRING scalar expression", "description": "Process instance actually producing the activity Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **OFFICE_DESCENDANT_ACTIVITY_CTX_TEXT** — {"type": "STRING scalar expression", "description": "Activity detail: actual command, downloaded object/URL or file path as appropriate; retain the original event reference Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **OFFICE_DESCENDANT_ACTIVITY_CTX_OUTCOME** — {"type": "STRING scalar expression", "description": "Actual operation outcome; UNKNOWN when absent, never success by default Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}

[Unfilled bindings file](../bindings/n027.bindings.example.json). All binding values intentionally start as null.

## Pipeline and result semantics

Read the stages in order: the source and early filters establish the population; alter creates explicitly typed aliases; joins/union introduce related evidence; comp/windowcomp/dedup change row cardinality where present; the final projection, sort and limit define displayed rows. A result limit does not bound scanning or join work. Check the identity and time predicates in each subquery against the binding contract.

Estimated cost: MEDIUM. Bounded lookback and early filters; result caps do not bound upstream scanning. No measured performance claim.

## Limitations and tuning

- Office-launched shells can be authorized. Descendant events outside the ten-minute window or retained ancestry depth are excluded.
- One anchor can have many context rows; cross-endpoint or reused numeric PID matches are disallowed.
- No tenant compilation or execution. Validate nulls, access, retention and source collection before interpreting empty results.
- A terminal limit bounds displayed output, not upstream scans or join multiplicity.

## Acceptance scenarios — NOT RUN

- {"kind": "positive", "fixture": "Office directly creates cmd P1; a verified descendant writes a file 30 seconds later", "expected": "Anchor and FILE_WRITE evidence rows."}
- {"kind": "negative", "fixture": "cmd has an unrelated direct parent or a same-PID process on another endpoint", "expected": "No Office anchor or false cross-host context."}
- {"kind": "null", "fixture": "Valid anchor with missing descendant telemetry", "expected": "Anchor remains; no invented downstream success."}
- {"kind": "edge", "fixture": "Context exactly 600 seconds after the anchor, and a second event at 601", "expected": "Only the 600-second context is admitted."}

These scenarios are plans for representative source fixtures and bounded tenant runs. They have not been executed in Cortex. First inspect the bound schema, then compile the complete body, then exercise positive, benign, negative, null and boundary cases. Do not promote this interactive hunt directly to BIOC/correlation without reviewing that surface.

Independent static review: **PASS**. Full-query C/E: **NOT RUN / NOT RUN**. Static review cannot establish source population or detection effectiveness.

## ATT&CK context

{"id": "T1059.003", "technique": "Windows Command Shell", "tactics": "Execution (TA0002)"}

Source workbook ATT&CK Enterprise v19.2 hypothesis retained for traceability. The query observes only the behavior in its coverage boundary; mapping is not certified detection coverage.

## References

- [Official source for TH-002](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields)
- [Official source for TH-002](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/action-actor)
- [Official source for TH-002](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/actor-actor)
- [Official source for TH-002](https://cortex-docs.paloaltonetworks.com/xql-command-reference-guide/readme/functions/timestamp_diff)
- [Official source for TH-002](https://docs-cortex.paloaltonetworks.com/r/Cortex/Cortex-XQL-Command-Reference/Example-2-Left-join?contentId=5iT66RH4ney5McWya7JKjQ)
- [Official source for TH-002](https://docs-cortex.paloaltonetworks.com/r/Cortex/Cortex-XQL-Command-Reference/Example-2-Union-with-an-Inner-XQL-Query)
- [Official source for TH-002](https://learn.microsoft.com/en-us/sysinternals/downloads/sysmon)
- [Official source for TH-002](https://attack.mitre.org/techniques/T1059/003/)
- [Cortex XDR union: process-start and file-write example](https://docs-cortex.paloaltonetworks.com/r/Cortex-XDR/Cortex-XDR-3.x-Documentation/union)
