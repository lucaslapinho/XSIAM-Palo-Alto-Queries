# N045 — Browser Credential Store Read Access - Schema Binding Template

Finds successful reads of inventoried browser credential-store files and preserves reader identity for review. Requires real access telemetry and time-valid browser-profile paths.

Proposal: **TH-020**. Category: **Threat Hunting / Credential Access**.

Artifact: **SCHEMA_BINDING_TEMPLATE**. Product/surface: Cortex XSIAM / Interactive XQL Search. Tenant release, full-query compiler acceptance and execution: **UNVERIFIED / NOT RUN**.

[Query file](../queries/threat-hunting/credential-access/n045-browser-credential-store-read-access-schema-binding-template.xql)

## Objective and interpretation

One observed read paired with one valid credential-store inventory entry; contract requires nonoverlapping inventory intervals.

Output grain: One observed read paired with one valid credential-store inventory entry; contract requires nonoverlapping inventory intervals.
Timeframe: 1d unless explicitly expanded in body

## Source and field contract

Sources: `["{{FILE_ACCESS_DATASET}}", "{{BROWSER_STORE_INVENTORY}}"]`.

Every schema token must resolve to an inspected field, expression or source with the stated type and meaning. A normalized source contract may require telemetry ingestion, parsing or a separately reviewed view; replacing names alone cannot create missing evidence. Preserve nulls. Verify timezone, identity namespaces, provider/channel, event outcomes, source completeness and row multiplicity.

- **FILE_ACCESS_DATASET** — {"type": "DATASET_IDENTIFIER", "kind": "DATASET", "binding_contract": "Audited file READ/access telemetry with result and process attribution; file-write-only events cannot satisfy this binding.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **READ_TIME** — {"type": "DATETIME", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "File operation source time.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **READ_EVENT_UID** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Unique file access event key. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **READ_ENDPOINT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Stable host ID. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **READ_PROCESS_INSTANCE** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Host/boot-scoped process instance key. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **READ_PROCESS_IMAGE** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Observed reader image path. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **READ_ACCOUNT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Reader principal. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **READ_CANONICAL_PATH** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Canonical absolute file path under a known browser profile; honor platform path case semantics. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder. Windows case-insensitive path components require the same reviewed canonical form; Unix case-sensitive paths must remain distinct.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **READ_OPERATION** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Normalize actual read-content operations to read; open/query/write remain distinct. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **READ_OUTCOME** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Normalize confirmed successful read operation to success; unknown remains null. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **BROWSER_STORE_INVENTORY** — {"type": "DATASET_IDENTIFIER", "kind": "DATASET", "binding_contract": "Versioned credential-store path inventory per endpoint and browser/profile; include only validated browser storage locations.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **STORE_ENDPOINT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Stable endpoint key matching file telemetry. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **STORE_PATH** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Canonical full credential-store path, same path normalization as file telemetry. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder. Windows case-insensitive path components require the same reviewed canonical form; Unix case-sensitive paths must remain distinct.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **STORE_BROWSER** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Browser product/version/profile context. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **STORE_VALID_FROM** — {"type": "DATETIME", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Inventory validity start.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **STORE_VALID_TO** — {"type": "DATETIME", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Exclusive inventory validity end; use a verified explicit future boundary for current entries.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}

[Unfilled bindings file](../bindings/n045.bindings.example.json). All binding values intentionally start as null.

## Pipeline and result semantics

Read the stages in order: the source and early filters establish the population; alter creates explicitly typed aliases; joins/union introduce related evidence; comp/windowcomp/dedup change row cardinality where present; the final projection, sort and limit define displayed rows. A result limit does not bound scanning or join work. Check the identity and time predicates in each subquery against the binding contract.

Estimated cost: HIGH. Bounded lookback and early filters; result caps do not bound upstream scanning. No measured performance claim.

## Limitations and tuning

- Browser, backup and security tools can read these files legitimately; baseline reader identity before escalation.
- Reads do not prove decryption, copying, archiving or credential exfiltration. Follow-on artifacts are investigation pivots, not implied outputs.
- No generic Login Data substring or write-only proxy substitutes for verified store paths and reads.
- All bindings are explicit contracts, not claims that a source or field exists. Bind field/expression placeholders to typed tenant fields or tested extraction expressions; never substitute a made-up name.
- One-day telemetry lookback unless stated otherwise. Source-clock, retention, collection and permission gaps can remove evidence; _time filtering must cover each source event time.
- All canonical normalized words in expressions are local mapping contracts, not undocumented Cortex enum values.
- Result limits apply after analysis and do not cap scanned data or join fan-out. No tenant compile, execution, performance or deployment claim.
- ATT&CK, where present, is an analytical interpretation requiring context, not proof of compromise.
- Inventory/coverage/snapshot source bindings must return every relevant validity record under the configured query timeframe; if their storage timestamps differ, establish and test an explicit inner timeframe before use. A missing lookup row is never automatically benign.
- Comparisons are case-sensitive. Binding owners must canonicalize values only in source namespaces proven case-insensitive (such as reviewed Windows service/path/account keys and DNS A-label names), identically on both join sides. Preserve Unix paths, case-sensitive identities, ARN resource components, and source evidence text. All normalized operation/result labels must match the documented local contract spelling exactly.
- Time-difference bounds are expressed at second resolution. Validate subsecond boundary behavior using source fixtures; direct timestamp comparisons enforce before/after ordering where required.

## Acceptance scenarios — NOT RUN

- {"kind": "positive", "fixture": "Reader P reads an inventoried Login Data path on host H inside its validity interval.", "expected": "One access record with browser/profile context.", "status": "NOT RUN"}
- {"kind": "negative", "fixture": "The same process modifies the file or reads an unrelated SQLite database.", "expected": "Excluded.", "status": "NOT RUN"}
- {"kind": "null", "fixture": "A read has no process instance or the inventory lacks the path.", "expected": "Excluded; incomplete attribution/coverage is recorded.", "status": "NOT RUN"}
- {"kind": "edge", "fixture": "A browser profile moves at noon; access uses the old path at 13:00.", "expected": "Old expired inventory entry cannot join.", "status": "NOT RUN"}

These scenarios are plans for representative source fixtures and bounded tenant runs. They have not been executed in Cortex. First inspect the bound schema, then compile the complete body, then exercise positive, benign, negative, null and boundary cases. Do not promote this interactive hunt directly to BIOC/correlation without reviewing that surface.

Independent static review: **PASS**. Full-query C/E: **NOT RUN / NOT RUN**. Static review cannot establish source population or detection effectiveness.

## ATT&CK context

{"id": "T1555.003", "technique": "Credentials from Web Browsers", "tactics": "Credential Access (TA0006)"}

Source workbook ATT&CK Enterprise v19.2 hypothesis retained for traceability. The query observes only the behavior in its coverage boundary; mapping is not certified detection coverage.

## References

- [MITRE browser credential stores](https://attack.mitre.org/techniques/T1555/003/)
- [Cortex XSIAM comp](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/comp?contentId=6TGIbW601nVvh2ycedN_hw)
- [Cortex XSIAM join](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/join?contentId=MyWFbdks6xgkqm_1lNkfGg)
- [Cortex XSIAM bin](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/bin?contentId=CU_u_bTkg_MICr5pUKivwg)
- [Cortex XSIAM sort](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/sort?contentId=a9tB71nqIWhA~Adcae6R7w)
