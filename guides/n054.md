# N054 — WinRM Session and Descendant Process Review - Schema Binding Template

Links authenticated WinRM shells to process starts through verified provider-instance ancestry, retaining requester, command and principal context within 30 minutes.

Proposal: **TH-030**. Category: **Threat Hunting / Lateral Movement**.

Artifact: **SCHEMA_BINDING_TEMPLATE**. Product/surface: Cortex XSIAM / Interactive XQL Search. Tenant release, full-query compiler acceptance and execution: **UNVERIFIED / NOT RUN**.

[Query file](../queries/threat-hunting/lateral-movement/n054-winrm-session-and-descendant-process-review-schema-binding-template.xql)

## Objective and interpretation

One authenticated WinRM shell and attributed descendant start; provider reuse across shells requires unique shell-to-process linkage in the contract.

Output grain: One authenticated WinRM shell and attributed descendant start; provider reuse across shells requires unique shell-to-process linkage in the contract.
Timeframe: 1d unless explicitly expanded in body

## Source and field contract

Sources: `["{{WINRM_SESSION_DATASET}}", "{{WINRM_DESCENDANT_PROCESS_DATASET}}"]`.

Every schema token must resolve to an inspected field, expression or source with the stated type and meaning. A normalized source contract may require telemetry ingestion, parsing or a separately reviewed view; replacing names alone cannot create missing evidence. Preserve nulls. Verify timezone, identity namespaces, provider/channel, event outcomes, source completeness and row multiplicity.

- **WINRM_SESSION_DATASET** — {"type": "DATASET_IDENTIFIER", "kind": "DATASET", "binding_contract": "Verified WinRM authentication/operational source preserving target, requester, shell/session identifier and result.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **WINRM_AUTH_TIME** — {"type": "DATETIME", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Successful authenticated session/shell creation time.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **WINRM_ENDPOINT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Destination endpoint. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **WINRM_SOURCE** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Authenticated remote client; document proxy boundaries. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **WINRM_ACCOUNT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Authenticated principal. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **WINRM_SHELL_KEY** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Stable endpoint/boot-scoped WinRM shell/session ID. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **WINRM_AUTH_RESULT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "success only for authenticated WinRM session acceptance. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **WINRM_PROVIDER_INSTANCE** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Verified provider-host process instance serving this exact shell, obtained from operational linkage. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **WINRM_DESCENDANT_PROCESS_DATASET** — {"type": "DATASET_IDENTIFIER", "kind": "DATASET", "binding_contract": "Process-start telemetry with validated ancestry mapping to the exact WinRM provider instance; not image-name equality alone.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **WINRM_PROCESS_TIME** — {"type": "DATETIME", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Descendant process start.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **WINRM_PROCESS_ENDPOINT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Same destination endpoint. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **WINRM_ANCESTOR_INSTANCE** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Actual ancestor/provider instance from validated ancestry graph; direct parent may suffice for first-generation scope. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **WINRM_PROCESS_INSTANCE** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Child/descendant process instance. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **WINRM_PROCESS_IMAGE** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Started process image path. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **WINRM_PROCESS_COMMAND** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Observed started-process command line. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **WINRM_PROCESS_USER** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Observed principal. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **WINRM_PROCESS_OPERATION** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "process_start only for starts. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}

[Unfilled bindings file](../bindings/n054.bindings.example.json). All binding values intentionally start as null.

## Pipeline and result semantics

Read the stages in order: the source and early filters establish the population; alter creates explicitly typed aliases; joins/union introduce related evidence; comp/windowcomp/dedup change row cardinality where present; the final projection, sort and limit define displayed rows. A result limit does not bound scanning or join work. Check the identity and time predicates in each subquery against the binding contract.

Estimated cost: HIGH. Bounded lookback and early filters; result caps do not bound upstream scanning. No measured performance claim.

## Limitations and tuning

- Port 5985/5986 or wsmprovhost naming alone cannot satisfy authentication and ancestry contracts.
- If a provider instance serves multiple simultaneous shells, bind a stricter proven shell/process relationship or hold the template; never attribute solely by host/time.
- Approved PowerShell remoting is common and should be compared with account/endpoint policy.
- All bindings are explicit contracts, not claims that a source or field exists. Bind field/expression placeholders to typed tenant fields or tested extraction expressions; never substitute a made-up name.
- One-day telemetry lookback unless stated otherwise. Source-clock, retention, collection and permission gaps can remove evidence; _time filtering must cover each source event time.
- All canonical normalized words in expressions are local mapping contracts, not undocumented Cortex enum values.
- Result limits apply after analysis and do not cap scanned data or join fan-out. No tenant compile, execution, performance or deployment claim.
- ATT&CK, where present, is an analytical interpretation requiring context, not proof of compromise.
- Inventory/coverage/snapshot source bindings must return every relevant validity record under the configured query timeframe; if their storage timestamps differ, establish and test an explicit inner timeframe before use. A missing lookup row is never automatically benign.
- Comparisons are case-sensitive. Binding owners must canonicalize values only in source namespaces proven case-insensitive (such as reviewed Windows service/path/account keys and DNS A-label names), identically on both join sides. Preserve Unix paths, case-sensitive identities, ARN resource components, and source evidence text. All normalized operation/result labels must match the documented local contract spelling exactly.
- Time-difference bounds are expressed at second resolution. Validate subsecond boundary behavior using source fixtures; direct timestamp comparisons enforce before/after ordering where required.

## Acceptance scenarios — NOT RUN

- {"kind": "positive", "fixture": "Successful shell S maps to provider instance P; its validated descendant starts within five minutes.", "expected": "Returned.", "status": "NOT RUN"}
- {"kind": "negative", "fixture": "A local PowerShell start or unrelated provider instance occurs on the same host.", "expected": "Excluded.", "status": "NOT RUN"}
- {"kind": "null", "fixture": "Shell-to-provider mapping is missing.", "expected": "Excluded; no execution attribution.", "status": "NOT RUN"}
- {"kind": "edge", "fixture": "One provider serves two concurrent shells with different users and descendants cannot be assigned.", "expected": "Binding fails acceptance until attribution ambiguity is resolved.", "status": "NOT RUN"}

These scenarios are plans for representative source fixtures and bounded tenant runs. They have not been executed in Cortex. First inspect the bound schema, then compile the complete body, then exercise positive, benign, negative, null and boundary cases. Do not promote this interactive hunt directly to BIOC/correlation without reviewing that surface.

Independent static review: **PASS**. Full-query C/E: **NOT RUN / NOT RUN**. Static review cannot establish source population or detection effectiveness.

## ATT&CK context

{"id": "T1021.006", "technique": "Windows Remote Management", "tactics": "Lateral Movement (TA0008)"}

Source workbook ATT&CK Enterprise v19.2 hypothesis retained for traceability. The query observes only the behavior in its coverage boundary; mapping is not certified detection coverage.

## References

- [Microsoft WinRM authentication](https://learn.microsoft.com/en-us/windows/win32/winrm/authentication-for-remote-connections)
- [Cortex action schema](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/action-actor)
- [Cortex actor schema](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/actor-actor)
- [Cortex XSIAM comp](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/comp?contentId=6TGIbW601nVvh2ycedN_hw)
- [Cortex XSIAM join](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/join?contentId=MyWFbdks6xgkqm_1lNkfGg)
- [Cortex XSIAM bin](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/bin?contentId=CU_u_bTkg_MICr5pUKivwg)
- [Cortex XSIAM sort](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/sort?contentId=a9tB71nqIWhA~Adcae6R7w)
