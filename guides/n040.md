# N040 — Threat Hunting - Domain Controller Database and Snapshot Activity

Builds a domain-controller timeline of actual NTDS path access/copy, raw reads of its volume, and relevant snapshot/database utility starts with outcome and backup context.

Proposal: **TH-015**. Category: **Threat Hunting / Credential Access**.

Artifact: **SCHEMA_BINDING_TEMPLATE**. Product/surface: Cortex XSIAM / Interactive XQL Search. Tenant release, full-query compiler acceptance and execution: **UNVERIFIED / NOT RUN**.

[Query file](../queries/threat-hunting/credential-access/n040-threat-hunting-domain-controller-database-and-snapshot-activity.xql)

## Objective and interpretation

Full access/copy/raw-volume/tooling review template. Each evidence category stays distinct; no filename/tool match is promoted to confirmed database extraction.

Output grain: See the query projection and aggregation.
Timeframe: 1d

## Source and field contract

Sources: `["{{DIRECTORY_DATABASE_ACTIVITY_DATASET}}", "{{DOMAIN_CONTROLLER_DATABASES_DATASET}}"]`.

Every schema token must resolve to an inspected field, expression or source with the stated type and meaning. A normalized source contract may require telemetry ingestion, parsing or a separately reviewed view; replacing names alone cannot create missing evidence. Preserve nulls. Verify timezone, identity namespaces, provider/channel, event outcomes, source completeness and row multiplicity.

- **DIRECTORY_DATABASE_ACTIVITY_DATASET** — {"type": "XQL dataset identifier", "description": "Actual read-only source for DIRECTORY_DATABASE_ACTIVITY. Bind a documented/inspected dataset or reviewed view; this name is not a claim of an existing integration.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **DIRECTORY_DATABASE_ACTIVITY_EVENT_TIME** — {"type": "DATETIME scalar expression", "description": "Observed activity time Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **DIRECTORY_DATABASE_ACTIVITY_HOST_ID** — {"type": "STRING scalar expression", "description": "Stable endpoint identity Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **DIRECTORY_DATABASE_ACTIVITY_OPERATION** — {"type": "STRING scalar expression", "description": "Normalized FILE_READ, FILE_COPY, RAW_VOLUME_READ or PROCESS_START based on real operation evidence Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **DIRECTORY_DATABASE_ACTIVITY_OBJECT_PATH** — {"type": "STRING scalar expression", "description": "Actual file path involved in read/copy; canonical Windows path Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **DIRECTORY_DATABASE_ACTIVITY_VOLUME_PATH** — {"type": "STRING scalar expression", "description": "Canonical target volume for raw-volume reads Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **DIRECTORY_DATABASE_ACTIVITY_DESTINATION_PATH** — {"type": "STRING scalar expression", "description": "Observed copy destination when explicitly recorded; null otherwise Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **DIRECTORY_DATABASE_ACTIVITY_PROCESS_NAME** — {"type": "STRING scalar expression", "description": "Producing/started process basename Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **DIRECTORY_DATABASE_ACTIVITY_PROCESS_ID** — {"type": "STRING scalar expression", "description": "Stable producing process instance Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **DIRECTORY_DATABASE_ACTIVITY_COMMAND_LINE** — {"type": "STRING scalar expression", "description": "Observed process command line Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **DIRECTORY_DATABASE_ACTIVITY_SUBJECT** — {"type": "STRING scalar expression", "description": "Actual actor identity Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **DIRECTORY_DATABASE_ACTIVITY_OUTCOME** — {"type": "STRING scalar expression", "description": "Operation outcome Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **DIRECTORY_DATABASE_ACTIVITY_BACKUP_CONTEXT** — {"type": "STRING scalar expression", "description": "Time/process-scoped approved backup/recovery reference if established; UNKNOWN otherwise Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **DOMAIN_CONTROLLER_DATABASES_DATASET** — {"type": "XQL dataset identifier", "description": "Actual read-only source for DOMAIN_CONTROLLER_DATABASES. Bind a documented/inspected dataset or reviewed view; this name is not a claim of an existing integration.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **DOMAIN_CONTROLLER_DATABASES_DC_HOST** — {"type": "STRING scalar expression", "description": "Authoritative DC endpoint identity; one applicable row per actual database path/volume Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **DOMAIN_CONTROLLER_DATABASES_DATABASE_PATH** — {"type": "STRING scalar expression", "description": "Actual NTDS database path, not assumed default Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **DOMAIN_CONTROLLER_DATABASES_DATABASE_VOLUME** — {"type": "STRING scalar expression", "description": "Actual database volume Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}
- **DOMAIN_CONTROLLER_DATABASES_DOMAIN_NAME** — {"type": "STRING scalar expression", "description": "Authoritative AD domain Map from the actual source schema; preserve null when unavailable, and validate normalization against raw records.", "required": true, "evidence_status": "UNVERIFIED / TENANT-UNVERIFIED", "substitution": "Replace the entire token with one validated XQL identifier, scalar expression or literal of the stated type; do not quote identifiers or splice arbitrary stages."}

[Unfilled bindings file](../bindings/n040.bindings.example.json). All binding values intentionally start as null.

## Pipeline and result semantics

Read the stages in order: the source and early filters establish the population; alter creates explicitly typed aliases; joins/union introduce related evidence; comp/windowcomp/dedup change row cardinality where present; the final projection, sort and limit define displayed rows. A result limit does not bound scanning or join work. Check the identity and time predicates in each subquery against the binding contract.

Estimated cost: MEDIUM. Bounded lookback and early filters; result caps do not bound upstream scanning. No measured performance claim.

## Limitations and tuning

- Actual database path/DC inventory and file-read or raw-read telemetry are required. Ordinary file-create telemetry is not a substitute for reads.
- A raw volume read can target unrelated bytes; utility starts can be legitimate backup/recovery.
- No tenant compilation or execution. Validate nulls, access, retention and source collection before interpreting empty results.
- A terminal limit bounds displayed output, not upstream scans or join multiplicity.

## Acceptance scenarios — NOT RUN

- {"kind": "positive", "fixture": "A verified DC database path is read, then copied to a recorded destination", "expected": "Both actual operations appear with producing process/outcome."}
- {"kind": "negative", "fixture": "Same filename on a non-DC or unrelated file read on a DC", "expected": "Excluded."}
- {"kind": "null", "fixture": "Raw-volume event has no destination file", "expected": "Raw-volume lead retained without invented copy destination."}
- {"kind": "edge", "fixture": "DC uses a nondefault NTDS path or separate volume", "expected": "Actual inventory path/volume is used; no default-path blind spot."}

These scenarios are plans for representative source fixtures and bounded tenant runs. They have not been executed in Cortex. First inspect the bound schema, then compile the complete body, then exercise positive, benign, negative, null and boundary cases. Do not promote this interactive hunt directly to BIOC/correlation without reviewing that surface.

Independent static review: **PASS**. Full-query C/E: **NOT RUN / NOT RUN**. Static review cannot establish source population or detection effectiveness.

## ATT&CK context

{"id": "T1003.003", "technique": "NTDS", "tactics": "Credential Access (TA0006)"}

Source workbook ATT&CK Enterprise v19.2 hypothesis retained for traceability. The query observes only the behavior in its coverage boundary; mapping is not certified detection coverage.

## References

- [Official source for TH-015](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields)
- [Official source for TH-015](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/action-actor)
- [Official source for TH-015](https://cortex-docs.paloaltonetworks.com/xql-schema-reference/xdr-data-fields-by-actor/actor-actor)
- [Official source for TH-015](https://cortex-docs.paloaltonetworks.com/xql-command-reference-guide/readme/functions/timestamp_diff)
- [Official source for TH-015](https://docs-cortex.paloaltonetworks.com/r/Cortex/Cortex-XQL-Command-Reference/Example-2-Left-join?contentId=5iT66RH4ney5McWya7JKjQ)
- [Official source for TH-015](https://learn.microsoft.com/en-us/previous-versions/windows/it-pro/windows-10/security/threat-protection/auditing/event-4663)
- [Official source for TH-015](https://learn.microsoft.com/en-us/sysinternals/downloads/sysmon)
- [Official source for TH-015](https://attack.mitre.org/techniques/T1003/003/)
