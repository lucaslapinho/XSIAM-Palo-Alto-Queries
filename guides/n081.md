# N081 — External Permitted Traffic Outside Critical Server Access Paths - Schema Binding Template

Finds policy-permitted traffic from authoritatively external zones to time-valid critical servers when the exact source/server/protocol/port path is absent from a complete approved-path inventory.

Proposal: **OP-010**. Category: **Investigation / Critical Assets**.

Artifact: **SCHEMA_BINDING_TEMPLATE**. Product/surface: Cortex XSIAM / Interactive XQL Search. Tenant release, full-query compiler acceptance and execution: **UNVERIFIED / NOT RUN**.

[Query file](../queries/investigation/critical-assets/n081-external-permitted-traffic-outside-critical-server-access-paths-schema-binding-template.xql)

## Objective and interpretation

One firewall traffic record outside the reviewed allowed-path set for a valid critical server; policy permit is network evidence only.

Output grain: One firewall traffic record outside the reviewed allowed-path set for a valid critical server; policy permit is network evidence only.
Timeframe: 1d unless explicitly expanded in body

## Source and field contract

Sources: `["{{INBOUND_FIREWALL_TRAFFIC}}", "{{CRITICAL_SERVER_IP_INVENTORY}}", "{{APPROVED_CRITICAL_ACCESS_PATHS}}"]`.

Every schema token must resolve to an inspected field, expression or source with the stated type and meaning. A normalized source contract may require telemetry ingestion, parsing or a separately reviewed view; replacing names alone cannot create missing evidence. Preserve nulls. Verify timezone, identity namespaces, provider/channel, event outcomes, source completeness and row multiplicity.

- **INBOUND_FIREWALL_TRAFFIC** — {"type": "DATASET_IDENTIFIER", "kind": "DATASET", "binding_contract": "Verified firewall traffic with policy-permit outcome and original/post-NAT tuples; no port or network success proxy.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **INBOUND_TIME** — {"type": "DATETIME", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Source-generated firewall event timestamp.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **INBOUND_UID** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Unique source log record key. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **INBOUND_FIREWALL** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Stable firewall identifier. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **INBOUND_SOURCE_ZONE** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Configured source zone. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **INBOUND_DEST_ZONE** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Configured destination zone. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **INBOUND_SOURCE_IP** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Original client IP. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **INBOUND_DEST_IP** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Original destination IP. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **INBOUND_TRANSLATED_DEST** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Post-DNAT destination IP, normalized null when NAT is not applied; do not leave sentinel 0.0.0.0 as a real destination. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **INBOUND_DEST_PORT** — {"type": "INTEGER", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Post-DNAT destination port when translated, otherwise original; same boundary as approved path contract.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **INBOUND_PROTOCOL** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Canonical transport name matching path inventory. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **INBOUND_ACTION_CLASS** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "allow only for verified policy-permit action; not authenticated user success. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **INBOUND_RULE_ID** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Source-scoped firewall rule ID/name. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **EXTERNAL_ZONE_KEYS** — {"type": "QUOTED_STRING_LIST", "kind": "VALUE_OR_LITERAL", "binding_contract": "Authoritatively classified external-origin firewall\|source-zone pairs. Do not infer external origin from non-RFC1918 alone.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CRITICAL_SERVER_IP_INVENTORY** — {"type": "DATASET_IDENTIFIER", "kind": "DATASET", "binding_contract": "Versioned authoritative critical-server role/IP ownership inventory at the post-NAT server boundary.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CRITICAL_SERVER_IP** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Canonical owned IP matching resolved destination boundary. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CRITICAL_SERVER_ENDPOINT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Stable server ID. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CRITICAL_SERVER_NAME** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Server label. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CRITICAL_SERVER_FLAG** — {"type": "BOOLEAN", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "True only for owner-designated critical server, not inferred from its IP or port.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CRITICAL_SERVER_FROM** — {"type": "DATETIME", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Inclusive role/IP validity.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **CRITICAL_SERVER_TO** — {"type": "DATETIME", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Exclusive role/IP validity.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **APPROVED_PATH_BASELINE_COMPLETE** — {"type": "BOOLEAN_LITERAL", "kind": "VALUE_OR_LITERAL", "binding_contract": "true only after owner verifies approved-path inventory is authoritative and complete for all admitted critical servers and external zone keys; false must produce no outside-policy claims.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **APPROVED_CRITICAL_ACCESS_PATHS** — {"type": "DATASET_IDENTIFIER", "kind": "DATASET", "binding_contract": "Authoritative explicit allowed-path inventory, one row per external zone/client range-resolved key/server/protocol/port and validity interval; reviewed completeness is mandatory.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **APPROVED_PATH_ID** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Unique approval/change-control record. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **APPROVED_PATH_ZONE_KEY** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "firewall\|source-zone key matching traffic. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **APPROVED_PATH_SOURCE_IP** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Canonical allowed source IP, or a materialized reviewed per-address expansion of an approved range. Wildcards are not silently supported. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **APPROVED_PATH_ENDPOINT** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Stable critical server endpoint. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **APPROVED_PATH_PROTOCOL** — {"type": "STRING", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Canonical transport. Preserve source case unless a reviewed namespace-specific canonicalization is required; apply matching normalization on both join sides. Unknown keys must be null, not empty or a shared placeholder.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **APPROVED_PATH_PORT** — {"type": "INTEGER", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Allowed destination port at the post-NAT server boundary.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **APPROVED_PATH_FROM** — {"type": "DATETIME", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Inclusive approval validity.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}
- **APPROVED_PATH_TO** — {"type": "DATETIME", "kind": "FIELD_OR_EXPRESSION", "binding_contract": "Exclusive approval validity.", "status": "UNVERIFIED", "tenant_validation": "NOT RUN"}

[Unfilled bindings file](../bindings/n081.bindings.example.json). All binding values intentionally start as null.

## Pipeline and result semantics

Read the stages in order: the source and early filters establish the population; alter creates explicitly typed aliases; joins/union introduce related evidence; comp/windowcomp/dedup change row cardinality where present; the final projection, sort and limit define displayed rows. A result limit does not bound scanning or join work. Check the identity and time predicates in each subquery against the binding contract.

Estimated cost: HIGH. Bounded lookback and early filters; result caps do not bound upstream scanning. No measured performance claim.

## Limitations and tuning

- An absent path is meaningful only after the explicit completeness gate is true; incomplete policy information must remain unknown.
- Exact-IP allowlist expansion must cover approved ranges and IPv6 correctly; do not expand huge ranges impractically or silently treat a range string as an IP.
- Pre/post-NAT address and port mapping must be verified. Ambiguous dynamic/shared IP ownership cannot establish a critical endpoint.
- Allowed traffic is not authentication, exploit success or unauthorized access without owner/context review.
- All bindings are explicit contracts, not claims that a source or field exists. Bind field/expression placeholders to typed tenant fields or tested extraction expressions; never substitute a made-up name.
- One-day telemetry lookback unless stated otherwise. Source-clock, retention, collection and permission gaps can remove evidence; _time filtering must cover each source event time.
- All canonical normalized words in expressions are local mapping contracts, not undocumented Cortex enum values.
- Result limits apply after analysis and do not cap scanned data or join fan-out. No tenant compile, execution, performance or deployment claim.
- ATT&CK, where present, is an analytical interpretation requiring context, not proof of compromise.
- Inventory/coverage/snapshot source bindings must return every relevant validity record under the configured query timeframe; if their storage timestamps differ, establish and test an explicit inner timeframe before use. A missing lookup row is never automatically benign.
- Comparisons are case-sensitive. Binding owners must canonicalize values only in source namespaces proven case-insensitive (such as reviewed Windows service/path/account keys and DNS A-label names), identically on both join sides. Preserve Unix paths, case-sensitive identities, ARN resource components, and source evidence text. All normalized operation/result labels must match the documented local contract spelling exactly.
- Time-difference bounds are expressed at second resolution. Validate subsecond boundary behavior using source fixtures; direct timestamp comparisons enforce before/after ordering where required.

## Acceptance scenarios — NOT RUN

- {"kind": "positive", "fixture": "Allowed traffic from an external zone reaches critical server H on post-NAT port 22; complete approved paths allow only source B and observed source is A.", "expected": "Returned.", "status": "NOT RUN"}
- {"kind": "negative", "fixture": "The exact source/server/protocol/port path is approved at event time, or action is deny.", "expected": "Excluded.", "status": "NOT RUN"}
- {"kind": "null", "fixture": "Criticality/ownership is unknown or approved-path completeness is false.", "expected": "No outside-approved-path result.", "status": "NOT RUN"}
- {"kind": "edge", "fixture": "An approval expires at noon and the traffic is at exactly noon.", "expected": "Expired approval does not match because validity end is exclusive.", "status": "NOT RUN"}

These scenarios are plans for representative source fixtures and bounded tenant runs. They have not been executed in Cortex. First inspect the bound schema, then compile the complete body, then exercise positive, benign, negative, null and boundary cases. Do not promote this interactive hunt directly to BIOC/correlation without reviewing that surface.

Independent static review: **PASS**. Full-query C/E: **NOT RUN / NOT RUN**. Static review cannot establish source population or detection effectiveness.

## ATT&CK context

N/A — operational investigation

Operational visibility does not imply adversary behavior.

## References

- [PAN-OS traffic log fields](https://docs.paloaltonetworks.com/ngfw/administration/monitoring/use-syslog-for-monitoring/syslog-field-descriptions/traffic-log-fields)
- [Cortex XSIAM comp](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/comp?contentId=6TGIbW601nVvh2ycedN_hw)
- [Cortex XSIAM join](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/join?contentId=MyWFbdks6xgkqm_1lNkfGg)
- [Cortex XSIAM bin](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/bin?contentId=CU_u_bTkg_MICr5pUKivwg)
- [Cortex XSIAM sort](https://docs-cortex.paloaltonetworks.com/r/Cortex-XSIAM/Cortex-XSIAM-3.x-Documentation/sort?contentId=a9tB71nqIWhA~Adcae6R7w)
